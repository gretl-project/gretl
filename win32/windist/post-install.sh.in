#!/bin/sh

prefix=@prefix@

# delete stuff that's not needed in a runtime-only Windows package
rm -rf ${prefix}/include
rm -rf ${prefix}/appdata
rm -rf ${prefix}/pkgconfig
rm -rf ${prefix}/share/man
rm -rf ${prefix}/*.a
rm -rf ${prefix}/*.la
rm -rf ${prefix}/plugins/*.a
rm -rf ${prefix}/plugins/*.la

