<?PSGML NOFILL programlisting example informalequation?>

  <chapter id="cmdref"><title>Gretl commands</title>

  <sect1 id="cmd-intro"><title>Introduction</title>

    <para>
      The commands defined below may be executed interactively in the
      command-line client program or in the console window of the GUI
      program.  They may also be placed in a <quote>script</quote> or
      batch file for non-interactive execution.
    </para>

    <para>
      The following notational conventions are used below:
    </para>

    <itemizedlist>

      <listitem>
	<para>
	  A <literal>typewriter font</literal> is used for material that you
	  would type directly, and also for internal names of variables.
	</para>
      </listitem>

      <listitem>
	<para>
	  Terms in a <replaceable>slanted font</replaceable> are place-holders:
	  you should substitute some specific replacement.  For example, you
	  might type <varname>income</varname> in place of the generic
	  <replaceable>xvar</replaceable>.</para>
      </listitem>

      <listitem>
	<para>
	  The construction <literal>[</literal> <replaceable>arg</replaceable>
	  <literal>]</literal> means that the argument
	  <replaceable>arg</replaceable> is optional: you may supply it or not
	  (but in any case don't type the brackets).
	</para>
      </listitem>

      <listitem>
	<para>
	  The phrase <quote>estimation command</quote> means a command that
	  generates estimates for a given model, for example
	  <command>ols</command>, <command>ar</command> or
	  <command>wls</command>.
	</para>
      </listitem>

    </itemizedlist>

  </sect1>

  <sect1 id="cmd-cmd">
    <title>Commands</title>

  &cmdlist;
  
  </sect1>

  <sect1 id="cmd-topics">
    <title>Commands by topic</title>

    <para>
      The following sections show the available commands grouped by topic.
    </para>

  &cmdtopics;
  
  </sect1>

  </chapter>

<!-- Keep this comment at the end of the file
Local variables:
sgml-default-dtd-file:"../manual.ced"
mode: xml
sgml-parent-document:("../shell.xml" "book" "chapter")
End:
-->

