<?PSGML NOFILL programlisting example informalequation?>

  <chapter id="getting-started"><title>Getting started</title>

  <sect1 id="starting-regression"><title>Let's run a
      regression</title>

    <para>This introduction is mostly angled towards the graphical
      client program; please see <xref linkend="cmdref"/> and
      <xref linkend="cli"/>  below for details on the command-line
      program, <application>gretlcli</application>.
    </para>

    <para>
      You can supply the name of a data file to open as an argument to
      <application>gretl</application>, but for the moment let's not
      do that: just fire up the program.<footnote><para>For
	  convenience I will refer to the graphical client program
	  simply as <application>gretl</application> in this manual.
	  Note, however, that the specific name of the program differs
	  according to the computer platform.  On Linux it is called
	  <filename>gretl_x11</filename> while on MS Windows it is
	  <filename>gretlw32.exe</filename>. On Linux systems a
	  wrapper script named <filename>gretl</filename> is also
	  installed &mdash; see also <xref
	    linkend="optarg"/>.</para></footnote> You should see a
      main window (which will hold information on the data set but
      which is at first blank) and various menus, some of them
      disabled at first.
    </para>

    <para>
    What can you do at this point?  You can browse the supplied data
      files (or databases), open a data file, create a new data file,
      read the help items, or open a command script.  For now let's
      browse the supplied data files.  Under the File menu choose
      <quote>Open data, sample file, Ramanathan&hellip;</quote>.  A
      second window should open, presenting a list of data files
      supplied with the package (see <xref linkend="fig-datafiles"/>).
      The numbering of the files corresponds to the chapter
      organization of Ramanathan (2002), which contains discussion of
      the analysis of these data. The data will be useful for practice
      purposes even without the text.
    </para>

    <figure id="fig-datafiles" float="1" pgwide="1">
      <title>Practice data files window</title>
      <screenshot>
	<screeninfo>Practice data files window</screeninfo>
	<graphic fileref="figures/datafiles" align="center"/>
      </screenshot>
    </figure>

    <para>
      If you select a row in this window and click on
      <quote>Info</quote> this opens a window showing information on
      the data set in question (for example, on the sources and
      definitions of the variables).  If you find a file that is of
      interest, you may open it by clicking on <quote>Open</quote>, or
      just double-clicking on the file name. For the moment let's open
      <filename>data3-6</filename>.
    </para>

    <tip>
      <para>In <application>gretl</application> windows containing
	lists, double-clicking on a line launches a default action for
	the associated list entry: e.g. displaying the values of a
	data series, opening a file.</para>
    </tip>

    <para>
      This file contains data pertaining to a classic econometric
      <quote>chestnut</quote>, the consumption function.  The data
      window should now display the name of the current data file, the
      overall data range and sample range, and the names of the
      variables along with brief descriptive tags &mdash; see
      <xref linkend="fig-mainwin"/>.
    </para>

    <figure id="fig-mainwin" float="1" pgwide="1">
      <title>Main window, with a practice data file open</title>
      <screenshot>
	<screeninfo>Main window</screeninfo>
	<graphic fileref="figures/mainwin" align="center"/>
      </screenshot>
    </figure>

    <para>
      OK, what can we do now?  Hopefully the various menu options
      should be fairly self explanatory.  For now we'll dip into the
      Model menu; a brief tour of all the main window menus is given
      in
    <xref linkend="menus"/> below.
    </para>

    <para>
    <application>gretl</application>'s Model menu offers numerous
      various econometric estimation routines.  The simplest and most
      standard is Ordinary Least Squares (OLS). Selecting OLS pops up
      a dialog box calling for a <emphasis>model
	specification</emphasis> &mdash; see <xref
	linkend="fig-selector"/>.
    </para>

    <figure id="fig-selector" float="1" pgwide="1">
      <title>Model specification dialog</title>
      <screenshot>
	<screeninfo>Model specification dialog</screeninfo>
	<graphic fileref="figures/selector" align="center"/>
      </screenshot>
    </figure>

    <para>
      To select the dependent variable, highlight the variable you
      want in the list on the left and click the <quote>Choose</quote>
      button that points to the Dependent variable slot.  If you check
      the <quote>Set as default</quote> box this variable will be
      pre-selected as dependent when you next open the model dialog
      box. Shortcut: double-clicking on a variable on the left selects
      it as dependent and also sets it as the default. To select
      independent variables, highlight them on the left and click the
      <quote>Add</quote> button (or click the right mouse button over
      the highlighted variable).  To select several variable in the
      list box, drag the mouse over them; to select several
      non-contiguous variables, hold down the <literal>Ctrl</literal>
      key and click on the variables you want.
    </para>

    <para>To run a regression with consumption as the dependent
      variable and income as independent, click <varname>Ct</varname>
      into the Dependent slot and add <varname>Yt</varname> to the
      Independent variables list.</para>

  </sect1>

  <sect1 id="est-output"><title>Estimation output</title>

    <para>
      Once you've specified a model, a window displaying the
      regression output will appear.  The output is reasonably
      comprehensive and in a standard format (<xref 
	  linkend="fig-modelwin"/>).
    </para>  

    <figure id="fig-modelwin" float="1" pgwide="1">
      <title>Model output window</title>
      <screenshot>
	<screeninfo>Model window</screeninfo>
	<graphic fileref="figures/modelwin" align="center"/>
      </screenshot>
    </figure>

    <para>The output window contains menus that allow you to inspect
      or graph the residuals and fitted values, and to run various
      diagnostic tests on the model.</para>  

    <para>For most models there is also an option to reprint the
      regression output in &latex; format.  You can print the results
      in a tabular format (similar to what's in the output window, but
      properly typeset) or as an equation, across the page.  For each
      of these options you can choose to preview the typeset product,
      or save the output to file for incorporation in a &latex;
      document.  Previewing requires that you have a functioning &tex;
      system on your computer.</para>

    <para>To import <application>gretl</application> output into a
      word processor, you may copy and paste from an output window,
      using its <guimenuitem>Edit</guimenuitem> menu (or Copy button,
      in some contexts) to the target program.  Many (not all)
      <application>gretl</application> windows offer the option of
      copying in RTF (Microsoft's <quote>Rich Text Format</quote>) or
      as &latex;. If you are pasting into a word processor, RTF may be
      a good option because the tabular formatting of the output is
      preserved.<footnote><para>Note that when you copy as RTF under
	  MS Windows, Windows will only allow you to paste the
	  material into applications that <quote>understand</quote>
	  RTF.  Thus you will be able to paste into MS Word, but not
	  into notepad.  Note also that there appears to be a bug in
	  some versions of Windows, whereby the paste will not work
	  properly unless the <quote>target</quote> application (e.g.
	  MS Word) is running prior to copying the material in
	  question.</para>
      </footnote>  Alternatively, you can save the output to a (plain
      text) file then import the file into the target program.  When
      you finish a <application>gretl</application> session you are
      given the option of saving all the output from the session to a
      single file.</para>

    <para>Note that on the <application>gnome</application> desktop
      and under MS Windows, the <guimenuitem>File</guimenuitem> menu
      includes a command to send the output directly to a
      printer.</para>

    <tip><para>When pasting or importing plain text
	<application>gretl</application> output into a word processor,
	select a monospaced or typewriter-style font (e.g. Courier) to
	preserve the output's tabular formatting. Select a small font
	(10-point Courier should do) to prevent the output lines from
	being broken in the wrong place.</para>
    </tip>

  </sect1>

  <sect1 id="menus"><title>The main window menus</title>

    <para>
      Reading left to right along the main window's menu bar, we find
      the File, Utilities, Session, Data, Sample, Variable, Model and
      Help menus.
    </para>

    <graphic fileref="figures/menubar" align="center"/>

    <itemizedlist>

      <listitem><para><guimenu>File menu</guimenu></para>
	<itemizedlist mark="dash">
	  <listitem><para><guimenuitem>Open data</guimenuitem>: Open a
	      native <application>gretl</application> data file or
	      import from other formats.  See <xref
		linkend="datafiles"/>.</para></listitem>
	  <listitem><para><guimenuitem>Append data</guimenuitem>: Add
	      data to the current working data set, from a
	      <application>gretl</application> data file, a
	      comma-separated values file or a spreadsheet
	      file.</para></listitem>
	  <listitem><para><guimenuitem>Save data</guimenuitem>: Save
	      the currently open native
	      <application>gretl</application> data
	      file.</para></listitem>
	  <listitem><para><guimenuitem>Save data as</guimenuitem>:
	      Write out the current data set in native format, with
	      the option of using gzip data compression. See <xref
		linkend="datafiles"/>.
	    </para></listitem>
	  <listitem><para><guimenuitem>Export data</guimenuitem>:
	      Write out the current data set in Comma Separated Values
	      (CSV) format, or the formats of GNU R or GNU Octave. See
	      <xref linkend="datafiles"/> and also <xref
		linkend="app-advanced"/>.</para></listitem>
	  <listitem><para><guimenuitem>Clear data set</guimenuitem>:
	      Clear the current data set out of memory.  Generally you
	      don't have to do this (since opening a new data file
	      automatically clears the old one) but sometimes it's
	      useful (see <xref
		linkend="scratch"/>).</para></listitem>
	  <listitem><para><guimenuitem>Browse databases</guimenuitem>:
	      See <xref linkend="dbase"/> and
	      <xref linkend="scratch"/>.</para></listitem>
	  <listitem><para><guimenuitem>Create data set</guimenuitem>:
	      Initialize the built-in spreadsheet for entering data
	      manually.  See <xref
		linkend="scratch"/>.</para></listitem>
	  <listitem><para><guimenuitem>View command log</guimenuitem>:
	      Open a window containing a record of the commands
	      executed so far.</para></listitem>
	  <listitem><para><guimenuitem>Open command
		file</guimenuitem>: Open a file of
	      <application>gretl</application> commands, either one
	      you have created yourself or one of the practice files
	      supplied with the package.  If you want to create a
	      command file from scratch use the next item,
	      <guimenuitem>New command
		file</guimenuitem>.</para></listitem>
	  <listitem><para><guimenuitem>Preferences</guimenuitem>: Set
	      the paths to various files
	      <application>gretl</application> needs to access. Choose
	      the font in which gretl displays text output.  Select or
	      unselect <quote>expert mode</quote>. (If this mode is
	      selected various warning messages are suppressed.)
	      Activate or suppress <application>gretl</application>'s
	      messaging about the availability of program updates.
	      Configure or turn on/off the main-window toolbar. See
	      <xref linkend="optarg"/> for
	      details.</para></listitem>
          <listitem><para><guimenuitem>Exit</guimenuitem>: Quit the
	      program. If expert mode is not selected you'll be
	      prompted to save any unsaved work.</para></listitem>
	</itemizedlist>
      </listitem>

      <listitem><para><guimenu>Utilities menu</guimenu></para>
	<itemizedlist mark="dash">
	  <listitem><para><guimenuitem>Statistical
		tables</guimenuitem>: Look up critical values for
	      commonly used distributions (normal or Gaussian,
	      <emphasis>t</emphasis>, chi-square,
	      <emphasis>F</emphasis> and
	      Durbin&ndash;Watson).</para></listitem>
	  <listitem><para><guimenuitem>p-value finder</guimenuitem>:
	      Open a window which enables you to look up p-values from
	      the Gaussian, <emphasis>t</emphasis>, chi-square,
	      <emphasis>F</emphasis> or gamma distributions. See also
	      the <command>pvalue</command> command in <xref
		linkend="cmdref"/>.</para></listitem>
	  <listitem><para><guimenuitem>Test statistic
		calculator</guimenuitem>: Calculate test statistics
	      and p-values for a range of common hypothesis tests
	      (population mean, variance and proportion; difference of
	      means, variances and proportions).  The relevant sample
	      statistics must be already available for entry into the
	      dialog box. For some simple tests that take as input
	      data series rather than pre-computed sample statistics,
	      see <quote>Difference of means</quote> and
	      <quote>Difference of variances</quote> under the Data
	      menu.</para></listitem>
	  <listitem><para><guimenuitem>Gretl console</guimenuitem>:
	      Open a <quote>console</quote> window into which you can
	      type commands as you would using the command-line
	      program, <application>gretlcli</application> (as opposed
	      to using point-and-click). See <xref linkend="cmdref"/>.
	    </para></listitem> 
	  <listitem><para><guimenuitem>Start Gnu R</guimenuitem>:
	      Start <application>R</application> (if it is installed
	      on your system), and load a copy of the data set
	      currently open in <application>gretl</application>.  See
	  <xref linkend="app-advanced"/>.
	    </para></listitem>
	  <listitem><para><guimenuitem>NIST test suite</guimenuitem>:
	      Check the numerical accuracy of
	      <application>gretl</application> against the reference
	      results for linear regression made available by the (US)
	      National Institute of Standards and Technology.</para>
	  </listitem>
	</itemizedlist>
      </listitem>

      <listitem><para><guimenu>Session menu</guimenu></para>
	<itemizedlist mark="dash">
	  <listitem><para><guimenuitem>Icon view</guimenuitem>: Open a
	      window showing the current
	      <application>gretl</application> session as a set of
	      icons.  For details see <xref
	      linkend="session"/>.
	    </para></listitem>
	  <listitem><para><guimenuitem>Open</guimenuitem>: Open a
	      previously saved session file.
	    </para></listitem>
	  <listitem><para><guimenuitem>Save</guimenuitem>: Save the
	      current session to file.
	    </para></listitem>
	  <listitem><para><guimenuitem>Save as</guimenuitem>: Save the
	      current session to file under a chosen name.
	    </para></listitem>
	</itemizedlist>
      </listitem>
 
      <listitem><para><guimenu>Data menu</guimenu></para>
	<itemizedlist mark="dash">
	  <listitem><para><guimenuitem>Display values</guimenuitem>:
	      pops up a window with a simple (not editable) printout
	      of the values of the variables (either all of them or a
	      selected subset).</para></listitem>
	  <listitem><para><guimenuitem>Edit values</guimenuitem>: pops
	      up a spreadsheet window where you can make changes, add
	      new variables, and extend the number of observations.
	      </para></listitem>
	  <listitem><para><guimenuitem>Sort variables</guimenuitem>:
	      Rearrange the listing of variables in the main window,
	      either by ID number or alphabetically by name.
	      </para></listitem>
	  <listitem><para><guimenuitem>Graph specified
		vars</guimenuitem>: Gives a choice between a time
	      series plot, a regular X&ndash;Y scatter plot, an
	      X&ndash;Y plot using impulses (vertical bars), an
	      X&ndash;Y plot <quote>with factor separation</quote>
	      (i.e. with the points colored differently depending to
	      the value of a given dummy variable), boxplots, and a
	      3-D graph. Serves up a dialog box where you specify the
	      variables to graph. See <xref linkend="chap-graphs"/>
	      for details.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Multiple
		scatterplots</guimenuitem>: Show a collection of (at
	      most six) pairwise plots, with either a given variable
	      on the <emphasis>y</emphasis> axis plotted against
	      several different variables on the
	      <emphasis>x</emphasis> axis, or several
	      <emphasis>y</emphasis> variables plotted against a given
	      <emphasis>x</emphasis>. May be useful for exploratory
	      data analysis.</para></listitem>
	  <listitem><para><guimenuitem>Read info</guimenuitem>,
	      <guimenuitem>Edit info</guimenuitem>: <quote>Read
		info</quote> just displays the summary information
	      for the current data file; <quote>Edit info</quote>
	      allows you to make changes to it (if you have permission
	      to do so).</para></listitem>
	  <listitem><para><guimenuitem>Print
		description</guimenuitem>: opens a window containing a
		full account of the current dataset, including the
		summary information and any specific information on
		each of the variables.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Summary
		statistics</guimenuitem>: shows a fairly full set of
	      descriptive statistics for all variables in the data
	      set.</para></listitem>
	  <listitem><para><guimenuitem>Correlation
		matrix</guimenuitem>: shows the pairwise correlation
	      coefficients for the variables in the data
	      set.</para></listitem>
	  <listitem><para><guimenuitem>Difference of
		means</guimenuitem>: calculates the
	      <emphasis>t</emphasis> statistic for the null hypothesis
	      that the population means are equal for two selected
	      variables and shows its p-value.</para></listitem>
	  <listitem><para><guimenuitem>Difference of
		variances</guimenuitem>: calculates the
	      <emphasis>F</emphasis> statistic for the null hypothesis
	      that the population variances are equal for two selected
	      variables and shows its p-value.</para></listitem>
	  <listitem><para><guimenuitem>Add variables</guimenuitem>
	      gives a sub-menu of standard transformations of
	      variables (logs, lags, squares, etc.) that you may wish
	      to add to the data set. Also gives the option of adding
	      random variables, and (for time-series data) adding
	      seasonal dummy variables (e.g. quarterly dummy variables
	      for quarterly data). Includes an item for seeding the
	      program's pseudo-random number
	      generator.</para></listitem>
	  <listitem><para><guimenuitem>Refresh window</guimenuitem>
	      Sometimes <application>gretl</application> commands
	      generate new variables.  The <quote>refresh</quote> item
	      ensures that the listing of variables visible in the
	      main data window is in sync with the program's internal
	      state.</para></listitem>
	</itemizedlist>
      </listitem>

      <listitem><para><guimenu>Sample menu</guimenu></para>
	<itemizedlist mark="dash">
	  <listitem><para><guimenuitem>Set range</guimenuitem>: Select
	      a different starting and/or ending point for the current
	      sample, within the range of data available.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Restore full
		range</guimenuitem>:
	      self-explanatory.</para></listitem>
	  <listitem><para><guimenuitem>Set frequency,
		startobs</guimenuitem>: Impose a particular
	      interpretation of the data in terms of frequency and
	      starting point.  This may be useful with
	      panel data; see <xref
		linkend="chap-panel"/>.</para>
	  </listitem> 
	  <listitem><para><guimenuitem>Compact data</guimenuitem>: For
	      time-series data of higher than annual frequency, gives
	      you the option of compacting the data to a lower
	      frequency, using one of four compaction methods
	      (average, sum, start of period or end of
	      period).</para></listitem>
	  <listitem><para><guimenuitem>Define, based on
		dummy</guimenuitem>: Given a dummy (indicator)
	      variable with values 0 or 1, this drops from the current
	      sample all observations for which the dummy variable has
	      value 0.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Restrict, based on
		criterion</guimenuitem>: Similar to the item above,
	      except that you don't need a pre-defined variable: you
	      supply a Boolean expression (e.g. <literal>sqft &gt;
		1400</literal>) and the sample is restricted to
	      observations satisfying that condition. See the help for
	      <command>genr</command> in <xref
	    linkend="cmdref"/> for details on the Boolean operators
	      that can be used.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Drop all obs with missing
		values</guimenuitem>: Drop from the current sample all
	      observations for which at least one variable has a
	      missing value (see <xref linkend="missing-data"/>).
	    </para>
	  </listitem> 
	  <listitem><para><guimenuitem>Count missing
		values</guimenuitem>: Give a report on observations
	      where data values are missing. May be useful in
	      examining a panel data set, where it's quite common to
	      encounter missing values.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Set missing value
		code</guimenuitem>: Set a numerical value that will be
	      interpreted as <quote>missing</quote> or <quote>not
		available</quote>.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Add case markers</guimenuitem>:
	      Prompts for the name of a text file containing
	      <quote>case markers</quote> (short strings identifying
	      the individual observations) and adds this information
	      to the data set. See
	      <xref linkend="datafiles"/>.</para>
	  </listitem>
	  <listitem><para><guimenuitem>Interpret as time 
		series</guimenuitem>: Opens a dialog box which
	      enables you to set a time-series interpretation for data
	      that were read in as undated.</para></listitem>
	  <listitem><para><guimenuitem>Interpret as 
		panel</guimenuitem>: Opens a dialog box which
	      enables you to fix the interpretation of a panel data
	      set as either stacked time series or stacked cross
	      sections (see <xref
		linkend="chap-panel"/>).</para></listitem>
	  <listitem><para><guimenuitem>Restructure 
		panel</guimenuitem>: Allows the conversion of a panel
		data set in stacked cross-section form into stacked
		time series.  (Unlike the previous item, this one
		actually changes the organization of the data.)
	    </para></listitem>
	</itemizedlist>
      </listitem>

      <listitem><para><guimenu>Variable menu</guimenu> Most items
	  under here operate on a single variable at a time.  The
	  <quote>active</quote> variable is set by highlighting it
	  (clicking on its row) in the main data window.  Most options
	  will be self-explanatory.  Note that you can rename a
	  variable and can edit its descriptive label under
	  <quote>Edit attributes</quote>. You can also <quote>Define a
	    new variable</quote> via a formula (e.g. involving some
	  function of one or more existing variables). For the syntax
	  of such formulae, look at the online help for
	  <quote>Generate variable syntax</quote> or see the
	  <command>genr</command> command in 
	  <xref linkend="cmdref"/>. One simple example:
	  <programlisting> 
	    foo = x1 * x2</programlisting> will create a new variable
	  <varname>foo</varname> as the product of the existing
	  variables <varname>x1</varname> and <varname>x2</varname>.
	  In these formulae, variables must be referenced by name, not
	  number.</para></listitem> 

      <listitem><para><guimenu>Model menu</guimenu> For details on the
	  various estimators offered under this menu please consult
	  <xref linkend="estimators"/> and <xref
	linkend="cmdref"/> below, and/or the online help under
	  <quote>Help, Estimation</quote>.  Also see <xref
	    linkend="chap-nls"/> regarding the estimation of nonlinear
	  models.</para></listitem>

      <listitem><para><guimenu>Help menu</guimenu>  Please use this as
	  needed! It gives details on the syntax required in various
	  dialog entries.</para></listitem>   

    </itemizedlist>

  </sect1>

  <sect1 id="toolbar"><title>The gretl toolbar</title>

    <para>At the bottom left of the main window sits the
      toolbar.</para>

    <graphic fileref="figures/toolbar" align="center"/>

    <para>The icons have the following functions, reading from left to
      right:</para>
    
    <orderedlist>
      <listitem><para>Launch a calculator program.  A convenience
	  function in case you want quick access to a calculator when
	  you're working in <application>gretl</application>.  The
	  default program is <filename>calc.exe</filename> under MS
	  Windows, or <filename>xcalc</filename> under the X window
	  system.  You can change the program under the <quote>File,
	    Preferences, General</quote> menu, <quote>Programs</quote>
	  tab.</para></listitem>
      <listitem><para>Start a new script.  Opens an editor window in
	  which you can type a series of commands to be sent to the
	  program as a batch.</para></listitem>
      <listitem><para>Open the gretl console.  A shortcut to the
	  <quote>Gretl console</quote> menu item (<xref
	    linkend="menus"/> above).</para></listitem>
      <listitem><para>Open the gretl session window.</para></listitem>
      <listitem><para>Open the <application>gretl</application>
	  website in your web browser.  This will work only if you are
	  connected to the Internet and have a properly configured
	  browser.</para></listitem>
      <listitem><para>Open the current version of this manual, in PDF
	  format.  As with the previous item, this requires an
	  Internet connection; it also requires that your browser
	  knows how to handle PDF files.</para></listitem>
      <listitem><para>Open the  help item for script commands syntax
	  (i.e. a listing with details of all available
	  commands).</para></listitem>
      <listitem><para>Open the dialog box for defining a
	  graph.</para></listitem>
      <listitem><para>Open the dialog box for estimating a model
	  using ordinary least squares.</para></listitem>
      <listitem><para>Open a window listing the datasets associated
	  with Ramanathan's <citetitle>Introductory
	    Econometrics</citetitle> (and also the datasets from
	  Jeffrey Wooldridge's text, if these are installed &mdash;
	  see <xref
	    linkend="optarg"/>).</para></listitem>
    </orderedlist>

    <para>
      If you don't care to have the toolbar displayed, you can turn it
      off under the <quote>File, Preferences, General</quote> menu. Go
      to the Toolbar tab and uncheck the <quote>show gretl
	toolbar</quote> box.</para>

    </sect1>

  </chapter>

<!-- Keep this comment at the end of the file
Local variables:
sgml-default-dtd-file:"../manual.ced"
mode: xml
sgml-parent-document:("../manual.xml" "book" "chapter")
End:
-->

