
<sect2 id="cmd-add" xreflabel="add"><title>add</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
<row><entry>Examples:</entry><entry><literal>add 5 7 9</literal></entry></row>
<row><entry/><entry><literal>xx yy zz</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Must be invoked after an estimation command.  The variables in
	<replaceable>varlist</replaceable> are added to the previous model and the
	new model estimated.  If more than one variable is added, the
	<emphasis>F</emphasis> statistic for the added variables will
	be printed (for the OLS procedure only) along with its
	p-value.  A p-value below 0.05 means that the coefficients are
	jointly significant at the 5 percent level. A number of
	internal variables may be retrieved using the <command>genr</command>
	command, provided <command>genr</command> is invoked directly after
	this command.
      </para>
<para>Menu path: Model window, /Tests/add variables</para>
</sect2>

<sect2 id="cmd-addto" xreflabel="addto"><title>addto</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>modelID</replaceable> <replaceable>varlist</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>addto 2 5 7 9</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Works like the <command>add</command> command, except that you
	specify a previous model (using its ID number, which is
	printed at the start of the model output) to take as the base
	for adding variables.  The example above adds variables number
	5, 7 and 9 to Model 2.
      </para>
<para>Menu path: Model window, /Tests/add variables</para>
</sect2>

<sect2 id="cmd-adf" xreflabel="adf"><title>adf</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>order</replaceable> <replaceable>varname</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>adf 2 x1</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	  Computes statistics for two Dickey&#x2013;Fuller tests.  In
	each case the null hypothesis is that the variable in question
	exhibits a unit root. The first is a
	<emphasis>t</emphasis>-test based on the model 
	  <informalequation><alt role="tex">\[(1-L)x_t=m+gx_{t-1}+\epsilon_t\]</alt><graphic fileref="figures/adf1"/></informalequation>
	  The null hypothesis is that
	<emphasis>g</emphasis> = 0. The second (augmented) test
	proceeds by estimating an unrestricted regression (with
	regressors a constant, a time trend, the first lag of the
	variable, and <replaceable>order</replaceable> lags of the
	first difference) and a restricted version (dropping the time
	trend and the first lag).  The test statistic is
	  <informalequation><alt role="tex">\[F_{2,T-k}=\frac{(ESS_r-ESS_u)/2}{ESS_u/(T-k)}\]</alt><graphic fileref="figures/adf2"/></informalequation>
	  where <emphasis>T</emphasis> is the
	sample size, <emphasis>k</emphasis> the number of parameters
	in the unrestricted model, and the subscripts
	<emphasis>u</emphasis> and <emphasis>r</emphasis> denote the
	unrestricted and restricted models respectively. Note that the
	critical values for these statistics are not the usual ones; a
	p-value range is printed, when it can be determined.
      </para>
<para>Menu path: /Variable/Augmented Dickey-Fuller test</para>
</sect2>

<sect2 id="cmd-ar" xreflabel="ar"><title>ar</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>lags</replaceable> ; <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>ar 1 3 4 ; y 0 x1 x2 x3</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Computes parameter estimates using the generalized
	Cochrane&#x2013;Orcutt iterative procedure (see Section 9.5 of
	Ramanathan). Iteration is terminated when successive error
	sums of squares do not differ by more than 0.005 percent or
	after 20 iterations. <replaceable>lags</replaceable> is a list
	of lags in the residuals, terminated by a semicolon. In the
	above example, the error term is specified as 
	<informalequation><alt role="tex">\[u_t = \rho_1u_{t-1} + \rho_3 u_{t-3} +      \rho_4 u_{t-4} + e_t\]</alt><graphic fileref="figures/corc"/></informalequation>
	<replaceable>depvar</replaceable> is the
	dependent variable and <replaceable>indepvars</replaceable> is
	the list of independent variables.  Residuals of the
	transformed regression are stored under the name
	<literal>uhat</literal>, which can be retrieved by
	<command>genr</command>.  A number of other internal variables
	may be retrieved using the <command>genr</command> command,
	provided <command>genr</command> is invoked after this
	command.
      </para>
<para>Menu path: /Model/Autoregressive estimation</para>
</sect2>

<sect2 id="cmd-arch" xreflabel="arch"><title>arch</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>order</replaceable> <replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>arch 4 y 0 x1 x2 x3</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Tests the model for ARCH (Autoregressive Conditional
	Heteroskedasticity) of the lag order specified in
	<replaceable>order</replaceable>, which must be an integer. If
	the LM test statistic has p-value below 0.10, then ARCH
	estimation is also carried out.  If the predicted variance of
	any observation in the auxiliary regression is not positive,
	then the corresponding squared residual is used instead.
	Weighted least square estimation is then performed on the
	original model.
      </para>
<para>Menu path: Model window, /Tests/ARCH</para>
</sect2>

<sect2 id="cmd-arma" xreflabel="arma"><title>arma</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>p</replaceable> <replaceable>q</replaceable> ; <replaceable>depvar</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--native</literal> (Use native plugin (default))</entry></row>
<row><entry/><entry><literal>--x-12-arima</literal> (use X-12-ARIMA for estimation)</entry></row>
<row><entry/><entry><literal>--verbose</literal> (print details of iterations)</entry></row>
<row><entry/><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
<row><entry>Examples:</entry><entry><literal>arma 1 2 ; y</literal></entry></row>
<row><entry/><entry><literal>arma 2 2 ; y --verbose</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Estimates a univariate ARMA (Autoregressive, Moving Average)
	model.  The integer values <replaceable>p</replaceable> and
	<replaceable>q</replaceable> represent the AR and MA orders
	respectively. 
      </para>

<para>The default is to use the <quote>native</quote> gretl ARMA
	function; the <literal>--x-12-arima</literal> flag calls for
	use of the <application>X-12-ARIMA</application> program
	instead (this will work only if the
	<application>X-12-ARIMA</application> package for gretl is
	installed).  
      </para>

<para>The options given above may be combined, except that the
	covariance matrix is not available when estimation is by
	<application>X-12-ARIMA</application>.</para>

<para>The native gretl ARMA algorithm is largely due to Riccardo
	<quote>Jack</quote> Lucchetti.  It uses a conditional maximum
	likelihood procedure, implemented via iterated least squares
	estimation of the outer product of the gradient (OPG)
	regression.  See <xref linkend="jack-arma"/> for    the logic of the procedure.  The AR coefficients are
	initialized using an OLS auto-regression, and the MA
	coefficients are initialized at zero.</para>

<para>The AIC value given in connection with ARMA models is
	calculated according to the definition used in
	<application>X-12-ARIMA</application>, namely
	  <informalequation><alt role="tex">\[\mbox{AIC}=-2L+2k\]</alt><graphic fileref="figures/aic"/></informalequation>
	  where <emphasis>L</emphasis> is the
	log-likelihood and <emphasis>k</emphasis> is the total number
	of parameters estimated. The <quote>frequency</quote> figure
	  printed in connection with AR and MA roots is the &#x3BB;
	  value that solves
	  <informalequation><alt role="tex">\[z=re^{i2\pi\lambda}\]</alt><graphic fileref="figures/lambda"/></informalequation>
	  where <emphasis>z</emphasis> is the root
	  in question and <emphasis>r</emphasis> is its modulus.
      </para>
<para>Menu path: /Variable/ARMA model</para><para>Other access: Main window pop-up menu (single selection)</para>
</sect2>

<sect2 id="cmd-boxplot" xreflabel="boxplot"><title>boxplot</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varname</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	This command is available in the gretl console, but not in the
	command-line program.</para>

<para>Boxplots (after Tukey and Chambers) display the
	distribution of a variable. The central box encloses the
	middle 50 percent of the data, i.e. it is bounded by the first
	and third quartiles. The <quote>whiskers</quote> extend to the
	minimum and maximum values.  A line is drawn across the box at
	the median.</para>
<para>Menu path: /Data/Graph specified vars/Boxplots</para>
</sect2>

<sect2 id="cmd-chow" xreflabel="chow"><title>chow</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>obs</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>chow 25</literal></entry></row>
<row><entry/><entry><literal>chow 1988:1</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Must follow an OLS regression.  Creates a dummy variable which
	equals 1 from the split point specified by
	<replaceable>obs</replaceable> to the end of the sample, 0
	otherwise, and also creates interaction terms between this
	dummy and the original independent variables.  An augmented
	regression is run including these terms and an
	<emphasis>F</emphasis> statistic is calculated, taking the
	augmented regression as the unrestricted and the original as
	restricted.  This statistic is appropriate for testing the
	null hypothesis of no structural break at the given split
	point.
      </para>
<para>Menu path: Model window, /Tests/Chow test</para>
</sect2>

<sect2 id="cmd-coeffsum" xreflabel="coeffsum"><title>coeffsum</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>indepvars</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>coeffsum xt xt_1 xr_2</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Must follow an regression.  Calculates the sum of the
	coefficients on the variables in the
	<replaceable>indepvars</replaceable> list.  Prints this sum
	along with its standard error and the p-value for the null
	hypothesis that the sum is zero.
      </para>
<para>Menu path: Model window, /Tests/sum of coefficients</para>
</sect2>

<sect2 id="cmd-coint" xreflabel="coint"><title>coint</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>order</replaceable> <replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>coint 2 y x</literal></entry></row>
<row><entry/><entry><literal>coint 4 y x1 x2</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	The Engle&#x2013;Granger cointegration test.  Carries out
	Augmented Dickey&#x2013;Fuller tests on the null hypothesis
	that each of the variables listed has a unit root, using the
	given lag order.  The cointegrating regression is estimated,
	and an ADF test is run on the residuals from this regression.
	The Durbin&#x2013;Watson statistic for the cointegrating
	regression is also given.  Note that none of these test
	statistics can be referred to the usual statistical tables.
      </para>
<para>Menu path: /Model/Cointegration test/Engle-Granger</para>
</sect2>

<sect2 id="cmd-coint2" xreflabel="coint2"><title>coint2</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>order</replaceable> <replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--verbose</literal> (print details of auxiliary regressions)</entry></row>
<row><entry>Examples:</entry><entry><literal>coint2 2 y x</literal></entry></row>
<row><entry/><entry><literal>coint2 4 y x1 x2 --verbose</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Carries out the Johansen trace test for cointegration among
	the listed variables for the given order. Critical values are
	computed via J. Doornik's gamma approximation (Doornik, 1998).
	For details of this test see Hamilton (1994, Chapter 20).
      </para>
<para>Menu path: /Model/Cointegration test/Johansen</para>
</sect2>

<sect2 id="cmd-corc" xreflabel="corc"><title>corc</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
<row><entry>Example:</entry><entry><literal>corc 1 0 2 4 6 7</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Computes parameter estimates using the Cochrane&#x2013;Orcutt
	iterative procedure (see Section 9.4 of Ramanathan) with
	<replaceable>depvar</replaceable> as the dependent variable
	and <replaceable>indepvars</replaceable> as the list of
	independent variables.  Iteration is terminated when
	successive estimates of the autocorrelation coefficient do not
	differ by more than 0.001 or after 20 iterations.  Residuals
	of this transformed regression are stored under the name
	<literal>uhat</literal>.  Various internal variables may be
	retrieved using the <command>genr</command> command, provided
	<command>genr</command> is invoked immediately after this
	command.
      </para>
<para>Menu path: /Model/Cochrane-Orcutt</para>
</sect2>

<sect2 id="cmd-corr" xreflabel="corr"><title>corr</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>corr y x1 x2 x3</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Prints the pairwise correlation coefficients for the variables
	in <replaceable>varlist</replaceable>, or for all variables in
	the data set if <replaceable>varlist</replaceable> is not
	given.
      </para>
<para>Menu path: /Data/Correlation matrix</para><para>Other access: Main window pop-up menu (multiple selection)</para>
</sect2>

<sect2 id="cmd-corrgm" xreflabel="corrgm"><title>corrgm</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>variable</replaceable> <replaceable>maxlag</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>corrgm x 12</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Prints the values of the autocorrelation function for the
	<replaceable>variable</replaceable> specified (either by name
	or number).  See Ramanathan, Section 11.7.  It is thus
	<inlineequation><alt role="tex">$\rho(u_t, u_{t-s})$</alt><inlinemediaobject><imageobject><imagedata align="center" fileref="figures/autocorr"/></imageobject></inlinemediaobject></inlineequation>
	where
	<emphasis>u<subscript>t</subscript></emphasis> 
	is the <emphasis>t</emphasis>th observation of the variable
	<emphasis>u</emphasis> and <emphasis>s</emphasis> is the
	number of lags.
      </para>

<para>
	The partial autocorrelations are also shown: these are net of
	the effects of intervening lags.  The command also graphs the
	correlogram and prints the Box&#x2013;Pierce
	<emphasis>Q</emphasis> statistic for testing the null hypothesis
	that the series is <quote>white noise</quote>. This is
	asymptotically distributed as chi-square with degrees of
	freedom equal to the number of lags used.
      </para>

<para>
	If an (optional) integer <replaceable>maxlag</replaceable>
	value is supplied the length of the correlogram is limited to
	at most that number of lags, otherwise the length is
	determined automatically.
      </para>
<para>Menu path: /Variable/Correlogram</para><para>Other access: Main window pop-up menu (single selection)</para>
</sect2>

<sect2 id="cmd-criteria" xreflabel="criteria"><title>criteria</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>ess</replaceable> <replaceable>T</replaceable> <replaceable>k</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>criteria 23.45 45 8</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Computes the model selection statistics (see Ramanathan,
	Section 4.3), given <replaceable>ess</replaceable> (error sum
	of squares), the number of observations
	(<emphasis>T</emphasis>), and the number of coefficients
	(<emphasis>k</emphasis>). <emphasis>T</emphasis>,
	<emphasis>k</emphasis>, and <replaceable>ess</replaceable> may
	be numerical values or names of previously defined variables.
      </para>
<para>Menu path: None</para>
</sect2>

<sect2 id="cmd-critical" xreflabel="critical"><title>critical</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>dist</replaceable> <replaceable>param1</replaceable> <literal>[ </literal><replaceable>param2</replaceable> <literal>] </literal></entry></row>
<row><entry>Examples:</entry><entry><literal>critical t 20</literal></entry></row>
<row><entry/><entry><literal>critical X 5</literal></entry></row>
<row><entry/><entry><literal>critical F 3 37</literal></entry></row>
</tbody></tgroup></informaltable>

<para>If <replaceable>dist</replaceable> is
	<literal>t</literal>, <literal>X</literal> or
	<literal>F</literal>, prints out the critical values for the
	student's <emphasis>t</emphasis>, chi-square or
	<emphasis>F</emphasis> distribution respectively, for the
	common significance levels and using the specified degrees of
	freedom, given as <replaceable>param1</replaceable> for t and
	chi-square, or <replaceable>param1</replaceable> and
	<replaceable>param2</replaceable> for <emphasis>F</emphasis>.
	If <replaceable>dist</replaceable> is <literal>d</literal>,
	prints the upper and lower values of the Durbin-Watson
	statistic at 5 percent significance, for the given number of
	observations, <replaceable>param1</replaceable>, and for the
	range of 1 to 5 explanatory variables.
      </para>
<para>Menu path: /Utilities/Statistical tables</para>
</sect2>

<sect2 id="cmd-cusum" xreflabel="cusum"><title>cusum</title>

<para>
	Must follow the estimation of a model via OLS.  Performs the
	CUSUM test for parameter stability.  A series of (scaled)
	one-step ahead forecast errors is obtained by running a series
	of regressions: the first regression uses the first
	<emphasis>k</emphasis> observations and is used to generate a
	prediction of the dependent variable at observation at
	observation <emphasis>k</emphasis> + 1; the second uses the
	first <emphasis>k</emphasis> + 1 observations and generates a
	prediction for observation <emphasis>k</emphasis> + 2, and so
	on (where <emphasis>k</emphasis> is the number of parameters
	in the original model).  The cumulated sum of the scaled
	forecast errors is printed and graphed.  The null hypothesis
	of parameter stability is rejected at the 5 percent
	significance level if the cumulated sum strays outside of the
	95 percent confidence band.
      </para>

<para>
	The Harvey&#x2013;Collier <emphasis>t</emphasis>-statistic for
	testing the null hypothesis of parameter stability is also
	printed.  See Chapter 7 of Greene's <citetitle>Econometric
	  Analysis</citetitle> for details.
      </para>
<para>Menu path: Model window, /Tests/CUSUM</para>
</sect2>

<sect2 id="cmd-data" xreflabel="data"><title>data</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>Reads the variables in <replaceable>varlist</replaceable>
	from a database (gretl or RATS 4.0), which must have been
	opened previously using the <command>open</command> command.
	In addition, a data frequency and sample range must be
	established using the the <command>setobs</command> and
	<command>smpl</command> commands prior to using this command.
	Here is a full example:</para>
<programlisting>
	open macrodat.rat
	setobs 4 1959:1
	smpl ; 1999:4
	data GDP_JP GDP_UK</programlisting>
<para>These commands open a database named
	<filename>macrodat.rat</filename>, establish a quarterly data
	set starting in the first quarter of 1959 and ending in the
	fourth quarter of 1999, and then import the series named
	<literal>GDP_JP</literal> and
	<literal>GDP_UK</literal>.</para>

<para>If the series to be read are of higher frequency than the
	working data set, you must specify a compaction method as
	below:</para>
<programlisting>
	data (compact=average) LHUR PUNEW</programlisting>
<para>The four available compaction methods are
	<quote>average</quote> (takes the mean of the high frequency
	observations), <quote>last</quote> (uses the last
	observation), <quote>first</quote> and <quote>sum</quote>.
      </para>
<para>Menu path: /File/Browse databases</para>
</sect2>

<sect2 id="cmd-delete" xreflabel="delete"><title>delete</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>Removes the listed variables (given by name or number)
	from the dataset. Use with caution: no
	confirmation is asked, and any variables with higher ID
	numbers will be re-numbered.</para>

<para>If no <replaceable>varlist</replaceable> is given with
	this command, it deletes the last (highest numbered) variable
	from the dataset.</para>
<para>Menu path: Main window pop-up (single selection)</para>
</sect2>

<sect2 id="cmd-diff" xreflabel="diff"><title>diff</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	The first difference of each variable in
	<replaceable>varlist</replaceable> is obtained and the result
	stored in a new variable with the prefix
	<literal>d_</literal>.  Thus <command>diff x y</command>
	creates the new variables <literal>d_x = x(t) -
	  x(t-1)</literal> and <literal>d_y = y(t) - y(t-1)</literal>.
      </para>
<para>Menu path: /Data/Add variables/first differences</para>
</sect2>

<sect2 id="cmd-else" xreflabel="else"><title>else</title>

<para>See <xref linkend="cmd-if"/>.</para>

</sect2>

<sect2 id="cmd-end" xreflabel="end"><title>end</title>

<para>
	Ends a block of commands of some sort.  For example,
	<command>end system</command> terminates an equation system.
      </para>

</sect2>

<sect2 id="cmd-endif" xreflabel="endif"><title>endif</title>

<para>See <xref linkend="cmd-if"/>.</para>

</sect2>

<sect2 id="cmd-endloop" xreflabel="endloop"><title>endloop</title>

<para>
	Marks the end of a command loop.  See <xref linkend="cmd-loop"/>.
      </para>

</sect2>

<sect2 id="cmd-eqnprint" xreflabel="eqnprint"><title>eqnprint</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><literal>[ </literal><replaceable>-f filename</replaceable> <literal>] </literal></entry></row>
<row><entry>Option:</entry><entry><literal>--complete</literal> (Create a complete document)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Must follow the estimation of a model.  Prints the estimated
	model in the form of a LaTeX equation.  If a filename is
	specified using the <literal>-f</literal> flag output goes to
	that file, otherwise it goes to a file with a name of the form
	<filename>equation_N.tex</filename>, where
	<literal>N</literal> is the number of models estimated to date
	in the current session.  See also <xref linkend="cmd-tabprint"/>.
      </para>

<para>
	If the <literal>--complete</literal> flag is given the LaTeX
	file is a complete document, ready for processing; otherwise
	it must be included in a document.
      </para>
<para>Menu path: Model window, /LaTeX</para>
</sect2>

<sect2 id="cmd-equation" xreflabel="equation"><title>equation</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>equation y x1 x2 x3 const</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Specifies an equation within a system of equations (see 
	<xref linkend="cmd-system"/>).  The syntax for specifying
	an equation is the same as that for, e.g., 
	<xref linkend="cmd-ols"/>.
      </para>

</sect2>

<sect2 id="cmd-fcast" xreflabel="fcast"><title>fcast</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><literal>[ </literal><replaceable>startobs endobs</replaceable> <literal>] </literal><replaceable>newvarname</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>fcast 1997:1 2001:4 f1</literal></entry></row>
<row><entry/><entry><literal>fcast fit2</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Must follow an estimation command.  Forecasts are generated
	for the specified range (or the largest possible range if no
	<replaceable>startobs</replaceable> and
	<replaceable>endobs</replaceable> are given) and the values
	saved as <replaceable>newvarname</replaceable>, which can be
	printed, graphed, or plotted. The right-hand side variables
	are those in the original model.  There is no provision to
	substitute other variables.  If an autoregressive error
	process is specified (for <command>hilu</command>,
	<command>corc</command>, and <command>ar</command>) the
	forecast is conditional one step ahead and incorporates the
	error process.
      </para>
<para>Menu path: Model window, /Model data/Forecasts</para>
</sect2>

<sect2 id="cmd-fcasterr" xreflabel="fcasterr"><title>fcasterr</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>startobs</replaceable> <replaceable>endobs</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--plot</literal> (display graph)</entry></row>
</tbody></tgroup></informaltable>

<para>
	After estimating an OLS model which includes a constant and at
	least one independent variable (these restrictions may be
	relaxed at some point) you can use this command to print out
	fitted values over the specified observation range, along with
	the estimated standard errors of those predictions and 95
	percent confidence intervals.  The augmented regression method
	of Salkever (1976) is used to generate the forecast standard
	errors.
      </para>
<para>Menu path: Model window, /Model data/Forecasts</para>
</sect2>

<sect2 id="cmd-fit" xreflabel="fit"><title>fit</title>

<para>
	Must follow an estimation command.  Generates fitted values,
	in a series called <literal>autofit</literal>, for the current
	sample, based on the last regression.  In the case of
	time-series models, also pops up a graph of fitted and
	actual values of the dependent variable against time.
      </para>

</sect2>

<sect2 id="cmd-freq" xreflabel="freq"><title>freq</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>var</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Prints the frequency distribution for <replaceable>var</replaceable> (given
	by name or number) along with the Doornik chi-square test for
	normality. In interactive mode a graph of the distribution is
	displayed.  
      </para>
<para>Menu path: /Variable/Frequency distribution</para>
</sect2>

<sect2 id="cmd-genr" xreflabel="genr"><title>genr</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>newvar</replaceable> <replaceable>= formula</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Creates new variables, usually through transformations of
	existing variables. See also <command>diff</command>,
	<command>logs</command>, <command>lags</command>,
	<command>ldiff</command>, <command>multiply</command> and
	<command>square</command> for shortcuts.
      </para>

<para>
	Supported arithmetical operators are, in
	order of precedence: <literal>^</literal> (exponentiation);
	<literal>*</literal>, <literal>/</literal> and
	<literal>%</literal> (modulus or remainder);
	<literal>+</literal> and <literal>-</literal>. 
      </para>

<para>
	The available Boolean operators are
	(again, in order of precedence): <literal>!</literal>
	(negation), <literal>&amp;</literal> (logical AND),
	<literal>|</literal> (logical OR), <literal>&gt;</literal>,
	<literal>&lt;</literal>, <literal>=</literal>,
	<literal>&gt;=</literal> (greater than or equal),
	<literal>&lt;=</literal> (less than or equal) and
	<literal>!=</literal> (not equal).  The Boolean operators can
	be used in constructing dummy variables: for instance
	<literal>(x &gt; 10)</literal> returns 1 if <literal>x</literal>
	&gt; 10, 0 otherwise. Supported functions
	fall into these groups:
      </para>
<itemizedlist><listitem>
<para>Standard math functions:
	    <function>abs</function>, <function>cos</function>,
	    <function>exp</function>, <function>int</function>
	    (integer part), <function>ln</function> (natural log:
	    <function>log</function> is a synonym),
	    <function>sin</function>,
	    <function>sqrt</function>.</para>
</listitem>
<listitem>
<para>Statistical functions:
	    <function>max</function> (maximum value in a series),
	    <function>min</function> (minimum),
	    <function>mean</function> (arithmetic mean),
	    <function>median</function>, <function>var</function>
	    (variance) <function>sd</function> (standard deviation),
	    <function>sst</function> (sum of squared deviations from
	    the mean), <function>sum</function>,
	    <function>cov</function> (covariance),
	    <function>corr</function> (correlation coefficient),
	    <function>pvalue</function>.</para>
</listitem>
<listitem>
<para>Time-series functions:
	    <function>diff</function> (first difference),
	    <function>ldiff</function> (log-difference, or first
	    difference of natural logs).  To generate lags of a
	    variable <literal>x</literal>, use the syntax
	    <command>x(-N)</command>, where <literal>N</literal>
	    represents the desired lag length; to generate leads, use
	    <command>x(+N)</command>;</para>
</listitem>
<listitem>
<para>Miscellaneous: <function>cum</function>
	    (cumulate), <function>sort</function>,
	    <function>uniform</function>, <function>normal</function>,
	    <function>misszero</function> (replace the missing
	    observation code in a given series with zeros),
	    <command>zeromiss</command> (the inverse operation to
	    <command>misszero</command>), <function>nobs</function>
	    (return the number of valid observations in a given data
	    series).</para>
</listitem>
</itemizedlist>

<para>
	All of the above functions with the exception of
	<function>cov</function>, <function>corr</function>,
	<function>pvalue</function>, <function>uniform</function> and
	<function>normal</function> take as their single argument
	either the name of a variable (note that you can't refer to
	variables by their ID numbers in a <command>genr</command>
	command) or a composite expression that evaluates to a
	variable (e.g. <literal>ln((x1+x2)/2)</literal>).
	<function>cov</function> and <function>corr</function> both
	require two arguments, and return respectively the covariance
	and the correlation coefficient between two named variables.
	The <function>pvalue</function> function takes the same
	arguments as the <command>pvalue</command> command (see
	below), but in this context commas should be placed between
	the arguments. <function>uniform()</function> and
	<function>normal()</function>, which do not take arguments,
	return pseudo-random series drawn from the uniform (0&#x2013;1)
	and standard normal distributions respectively (see also the
	<command>seed</command> command).  Uniform series are
	generated using the C library function
	<function>rand()</function>; for normal series the method of
	Box and Muller (1958) is used.</para>

<para>Besides the operators and functions just noted there are
	some special uses of <command>genr</command>:
      </para>
<itemizedlist><listitem>
<para><command>genr time</command> creates a time
	    trend variable (1,2,3,&#x2026;) called
	    <command>time</command>. <command>genr index</command>
	    does the same thing except that the variable is called
	    <literal>index</literal>.</para>
</listitem>
<listitem>
<para><command>genr dummy</command> creates dummy
	    variables up to the periodicity of the data.  E.g. in the
	    case of quarterly data (periodicity 4), the program
	    creates <literal>dummy_1</literal> = 1 for first quarter
	    and 0 in other quarters, <literal>dummy_2</literal> = 1
	    for the second quarter and 0 in other quarters, and so
	    on.</para>
</listitem>
<listitem>
<para><command>genr paneldum</command> creates a set
	    of special dummy variables for use with a panel data set
	    &#x2014; see <xref linkend="cmd-panel"/>.</para>
</listitem>
<listitem>
<para>Various internal variables defined in the
	    course of running a regression can be retrieved using
	    <command>genr</command>, as follows:</para>
</listitem>
</itemizedlist>

<para>
	Note: In the command-line program,
	<command>genr</command> commands that retrieve model-related
	data always reference the model that was estimated most
	recently.  This is also true in the GUI program, if one uses
	<command>genr</command> in the <quote>gretl console</quote> or
	enters a formula using the <quote>Define new variable</quote>
	option under the Variable menu in the main window.  With the
	GUI, however, you have the option of retrieving data from any
	model currently displayed in a window (whether or not it's the
	most recent model).  You do this under the <quote>Model
	  data</quote> menu in the model's window.</para>

<para>The internal series <literal>uhat</literal> and
	<literal>yhat</literal> hold, respectively, the residuals and
	fitted values from the last regression.</para>

<para>
	Three other <quote>internal</quote> variables are available:
	<literal>$nobs</literal> holds the number of observations in
	the current sample range (note that this may or may not equal
	the value of <literal>$T</literal>, the number of observations
	used in estimating the last model).  The variable
	<literal>t</literal> serves as an index of the observations.
	Thus for instance <literal>genr dum = (t=15)</literal> will
	generate a dummy variable that has value 1 for observation 15,
	0 otherwise.  The variable <literal>$pd</literal> holds the
	frequency or periodicity of the data (e.g. 4 for quarterly
	data).</para>
<itemizedlist><listitem>
<para>Suppose <literal>x</literal> is coded with
	    values 1, 2, or 3 and you want three dummy variables,
	    <literal>d1</literal> = 1 if <literal>x</literal> = 1, 0
	    otherwise, <literal>d2</literal> = 1 if
	    <literal>x</literal> = 2, and so on.  To create these, use
	    the commands:</para>
<programlisting>
	      genr d1 = (x=1)
	      genr d2 = (x=2)
	      genr d3 = (x=3)</programlisting></listitem>
<listitem>
<para>To create <literal>z</literal> =
	    <literal>max(x,y)</literal> do</para>
<programlisting>
	      genr d = x&gt;y
	      genr z = (x*d)+(y*(1-d))</programlisting></listitem>
</itemizedlist>
<para>Menu path: /Variable/Define new variable</para><para>Other access: Main window pop-up menu</para>
</sect2>

<sect2 id="cmd-gnuplot" xreflabel="gnuplot"><title>gnuplot</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>yvars</replaceable> <replaceable>xvar</replaceable> <literal>[ </literal><replaceable>dumvar</replaceable> <literal>] </literal></entry></row>
<row><entry>Options:</entry><entry><literal>--with-lines</literal> (use lines, not points)</entry></row>
<row><entry/><entry><literal>--with-impulses</literal> (use vertical lines)</entry></row>
<row><entry/><entry><literal>--suppress-fitted</literal> (don't show least squares fit)</entry></row>
<row><entry/><entry><literal>--dummy</literal> (see below)</entry></row>
<row><entry>Examples:</entry><entry><literal>gnuplot y1 y2 x</literal></entry></row>
<row><entry/><entry><literal>gnuplot x time --with-lines</literal></entry></row>
<row><entry/><entry><literal>gnuplot wages educ gender --dummy</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Without the <literal>--dummy</literal> option, the
	<replaceable>yvars</replaceable> are graphed against <replaceable>xvar</replaceable>. With
	<literal>--dummy</literal>, <replaceable>yvar</replaceable> is graphed
	against <replaceable>xvar</replaceable> with the points shown in different
	colors depending on whether the value of <replaceable>dumvar</replaceable>
	is 1 or 0.</para>

<para>
	The <literal>time</literal> variable behaves specially: if it
	does not already exist then it will be generated
	automatically.
      </para>

<para>
	In interactive mode the result is piped to gnuplot for
	display.  In batch mode a plot file named
	<filename>gpttmp01.plt</filename> is written.  (With
	subsequent uses of <command>gnuplot</command> similar files
	are created, with the number in the file name incremented.)
	The plots can be generated later using the command
	<command>gnuplot gpttmp.plt</command>.  (Under MS Windows,
	start <application>wgnuplot</application> and open the file
	<filename>gpttmp01.plt</filename>.)
      </para>

<para>A further option to this command is available: following
	the specification of the variables to be plotted and the
	option flag (if any), you may add literal gnuplot commands to
	control the appearance of the plot (for example, setting the
	plot title and/or the axis ranges).  These commands should be
	enclosed in braces, and each gnuplot command must be
	terminated with a semi-colon.  A backslash may be used to
	continue a set of gnuplot commands over more than one line.
	Here is an example of the syntax:
      </para>

<para>
	<literal>{ set title 'My Title'; set yrange [0:1000]; }</literal>
      </para>
<para>Menu path: /Data/Graph specified vars</para><para>Other access: Main window pop-up menu, graph button on
	toolbar</para>
</sect2>

<sect2 id="cmd-graph" xreflabel="graph"><title>graph</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>yvars</replaceable> <replaceable>xvar</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--tall</literal> (use 40 rows)</entry></row>
</tbody></tgroup></informaltable>

<para>
	ASCII graphics.  The <replaceable>yvars</replaceable> (which
	may be given by name or number) are  graphed against
	<replaceable>xvar</replaceable> using ASCII symbols. The
	<literal>--tall</literal> flag will produce a graph with 40
	rows and 60 columns. Without it, the graph will be 20 by 60
	(for screen output).  See also <xref linkend="cmd-gnuplot"/>.
      </para>

</sect2>

<sect2 id="cmd-hausman" xreflabel="hausman"><title>hausman</title>

<para>
	This test is available only after estimating a model using the
	<command>pooled</command> command (see also
	<command>panel</command> and <command>setobs</command>).  It
	tests the simple pooled model against the principal
	alternatives, the fixed effects and random effects
	models.</para>

<para>
	The fixed effects model adds a dummy variable for all but one
	of the cross-sectional units, allowing the intercept of the
	regression to vary across the units.  An
	<emphasis>F</emphasis>-test for the joint significance of
	these dummies is presented.  The random effects model
	decomposes the residual variance into two parts, one part
	specific to the cross-sectional unit and the other specific to
	the particular observation.  (This estimator can be computed
	only if the number of cross-sectional units in the data set
	exceeds the number of parameters to be estimated.) The
	Breusch&#x2013;Pagan LM statistic tests the null hypothesis
	(that the pooled OLS estimator is adequate) against the random
	effects alternative.</para>

<para>
	The pooled OLS model may be rejected against both of the
	alternatives, fixed effects and random effects. Provided the
	unit- or group-specific error is uncorrelated with the
	independent variables, the random effects estimator is more
	efficient than the fixed effects estimator; otherwise the
	random effects estimator is inconsistent and the fixed effects
	estimator is to be preferred. The null hypothesis for the
	Hausman test is that the group-specific error is not so
	correlated (and therefore the random effects model is
	preferable).  A low p-value for this test counts against the
	random effects model and in favor of fixed effects.
      </para>
<para>Menu path: Model window, /Tests/panel diagnostics</para>
</sect2>

<sect2 id="cmd-hccm" xreflabel="hccm"><title>hccm</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Presents OLS estimates with the heteroskedasticity consistent
	covariance matrix estimates for the standard errors of
	regression coefficients using MacKinnon and White (1985)
	<quote>jackknife</quote> estimates (see Ramanathan, Section
	8.3).
      </para>
<para>Menu path: /Model/HCCM</para>
</sect2>

<sect2 id="cmd-help" xreflabel="help"><title>help</title>

<para>
	Gives a list of available commands. <command>help</command>
	<replaceable>command</replaceable> describes <replaceable>command</replaceable> (e.g.
	<command>help smpl</command>).  You can type <command>man</command> instead of
	<command>help</command> if you like. 
      </para>
<para>Menu path: /Help</para>
</sect2>

<sect2 id="cmd-hilu" xreflabel="hilu"><title>hilu</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
</tbody></tgroup></informaltable>

<para>Computes parameter estimates using the Hildreth&#x2013;Lu
	search procedure (fine-tuned by the CORC procedure) with
	<replaceable>depvar</replaceable> as the dependent variable and
	<replaceable>indepvars</replaceable> as the list of independent variables.
	This procedure is designed to correct for serial correlation
	of the error term.  The error sum of squares of the
	transformed model is graphed against the value of rho from
	-0.99 to 0.99.
      </para>
<para>Menu path: /Model/Hildreth-Lu</para>
</sect2>

<sect2 id="cmd-hsk" xreflabel="hsk"><title>hsk</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Prints heteroskedasticity-corrected estimates (see Ramanathan,
	ch. 8) and associated statistics.  The auxiliary regression
	predicts the log of the square of residuals (using squares of
	independent variables but not their cross products) from which
	weighted least squares estimates are obtained.  
      </para>
<para>Menu path: /Model/Heteroskedasticity corrected</para>
</sect2>

<sect2 id="cmd-if" xreflabel="if"><title>if</title>

<para>Flow control for command execution.  The syntax is:</para>
<programlisting>
	if condition
           commands
        else
           commands
	endif
      </programlisting>
<para><replaceable>condition</replaceable> must be a Boolean expression, for
	the syntax of which see <xref linkend="cmd-genr"/>.  The
	<command>else</command> block is optional; <literal>if</literal>
	&#x2026; <literal>endif</literal> blocks may be nested.</para>

</sect2>

<sect2 id="cmd-import" xreflabel="import"><title>import</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>filename</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--box1</literal> (BOX1 data)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Brings in data from a comma-separated values (CSV) format
	file, such as can easily be written from a spreadsheet
	program.  The file should have variable names on the first
	line and a rectangular data matrix on the remaining lines.
	Variables should be arranged <quote>by observation</quote>
	(one column per variable; each row represents an observation).
	See  <xref linkend="datafiles"/>  for details.
      </para>

<para>
	With the <literal>--box1</literal> flag, reads a data file in
	BOX1 format, as can be obtained using the Data Extraction
	Service of the US Bureau of the Census.
      </para>
<para>Menu path: /File/Open data/import</para>
</sect2>

<sect2 id="cmd-info" xreflabel="info"><title>info</title>

<para>
      Prints out any supplementary information stored with the current
      datafile.
      </para>
<para>Menu path: /Data/Read info</para><para>Other access: Data browser windows</para>
</sect2>

<sect2 id="cmd-label" xreflabel="label"><title>label</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>varname</replaceable> <literal>-d </literal><replaceable>description</replaceable> <literal>-n </literal><replaceable>displayname</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>label x1 -d "Description of x1" -n "Graph name"</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Sets the descriptive label for the given variable (if the
	<literal>-d</literal> flag is given, followed by a string in
	double quotes) and/or the <quote>display name</quote> for the
	variable (if the <literal>-n</literal> flag is given, followed
	by a quoted string).  If a variable has a display name, this
	is used when generating graphs.</para>
<para>Menu path: /Variable/Edit attributes</para><para>Other access: Main window pop-up menu</para>
</sect2>

<sect2 id="cmd-labels" xreflabel="labels"><title>labels</title>

<para>
	Prints out the informative labels for any variables that have
	been generated using <command>genr</command>, and any labels
	added to the data set via the GUI.
      </para>

</sect2>

<sect2 id="cmd-lad" xreflabel="lad"><title>lad</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Calculates a regression that minimizes the sum of the absolute
	deviations of the observed from the fitted values of the
	dependent variable.  Coefficient estimates are derived using
	the Barrodale&#x2013;Roberts simplex algorithm; a warning is
	printed if the solution is not unique. Standard errors are
	derived using the bootstrap procedure with 500 drawings.
      </para>
<para>Menu path: /Model/Least Absolute Deviation</para>
</sect2>

<sect2 id="cmd-lags" xreflabel="lags"><title>lags</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Creates new variables which are lagged values of each of the
	variables in <replaceable>varlist</replaceable>.  The number of lagged
	variables equals the periodicity. For example, if the
	periodicity is 4 (quarterly), the command <command>lags x y</command>
	creates <literal>x_1</literal> = <literal>x(t-1)</literal>,
	<literal>x_2</literal> = <literal>x(t-2)</literal>,
	<literal>x_3</literal> = <literal>x(t-3)</literal> and
	<literal>x_4</literal> <literal>x(t-4)</literal>. Similarly
	for <literal>y</literal>. These variables must be referred to
	in the exact form, that is, with the underscore.
      </para>
<para>Menu path: /Data/Add variables/lags of selected variables</para>
</sect2>

<sect2 id="cmd-ldiff" xreflabel="ldiff"><title>ldiff</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	The first difference of the natural log of each variable in
	<replaceable>varlist</replaceable> is obtained and the result stored in a
	new variable with the prefix <literal>ld_</literal>.  Thus
	<command>ldiff x y</command> creates the new variables
	<literal>ld_x</literal> =
	  <inlineequation><alt role="tex">$\ln(x_t) - \ln(x_{t-1})$</alt><inlinemediaobject><imageobject><imagedata align="center" fileref="figures/ldx"/></imageobject></inlinemediaobject></inlineequation> and <literal>ld_y</literal> =
	  <inlineequation><alt role="tex">$\ln(y_t) - \ln(y_{t-1})$</alt><inlinemediaobject><imageobject><imagedata align="center" fileref="figures/ldy"/></imageobject></inlinemediaobject></inlineequation>.
      </para>
<para>Menu path: /Data/Add variables/log differences</para>
</sect2>

<sect2 id="cmd-leverage" xreflabel="leverage"><title>leverage</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Option:</entry><entry><literal>--save</literal> (save variables)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Must immediately follow an <command>ols</command> command. Calculates
	the leverage (<emphasis>h</emphasis>, which must lie in the
	range 0 to 1) for each data point in the sample on which the
	previous model was estimated.  Displays the residual
	(<emphasis>u</emphasis>) for each observation along with its
	leverage and a measure of its influence on the estimates, 
	  <inlineequation><alt role="tex">$uh/(1 - h)$</alt><inlinemediaobject><imageobject><imagedata align="center" fileref="figures/influence"/></imageobject></inlinemediaobject></inlineequation>. <quote>Leverage points</quote> for
	which the value of <emphasis>h</emphasis> exceeds
	2<emphasis>k</emphasis>/<emphasis>n</emphasis> (where
	<emphasis>k</emphasis> is the number of parameters being
	estmated and <emphasis>n</emphasis> is the sample size) are
	flagged with an asterisk.  For details on the concepts of
	leverage and influence see Davidson and MacKinnon (1993,
	Chapter 2).
      </para>

<para>
	DFFITS values are also shown: these are <quote>studentized
	  residuals</quote> (predicted residuals divided by their
	standard errors) multiplied by 
	  <inlineequation><alt role="tex">$\sqrt{h/(1 - h)}$</alt><inlinemediaobject><imageobject><imagedata align="center" fileref="figures/dffit"/></imageobject></inlinemediaobject></inlineequation>. For a discussion of studentized residuals
	and DFFITS see G. S. Maddala, <citetitle>Introduction to
	  Econometrics</citetitle>, chapter 12; also Belsley, Kuh and
	Welsch (1980).  Briefly, a <quote>predicted residual</quote>
	is the difference between the observed value of the dependent
	variable at observation <emphasis>t</emphasis>, and the fitted
	value for observation <emphasis>t</emphasis> obtained from a
	regression in which that observation is omitted (or a dummy
	variable with value 1 for observation <emphasis>t</emphasis>
	alone has been added); the studentized residual is obtained by
	dividing the predicted residual by its standard error.</para>

<para>If the <literal>--save</literal> flag is given with this
	command, then the leverage, influence and DFFITS values are
	added to the current data set.</para>
<para>Menu path: Model window, /Tests/influential observations</para>
</sect2>

<sect2 id="cmd-lmtest" xreflabel="lmtest"><title>lmtest</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Options:</entry><entry><literal>--logs</literal> (non-linearity, logs)</entry></row>
<row><entry/><entry><literal>--autocorr</literal> (serial correlation)</entry></row>
<row><entry/><entry><literal>--squares</literal> (non-linearity, squares)</entry></row>
<row><entry/><entry><literal>--white</literal> (heteroskedasticity (White's test))</entry></row>
</tbody></tgroup></informaltable>

<para>
	Must immediately follow an <command>ols</command> command. Carries out
	some combination of the following: Lagrange Multiplier tests
	for nonlinearity (logs and squares), White's test for
	heteroskedasticity, and the LMF test for serial correlation up
	to the periodicity (see Kiviet, 1986).  The corresponding
	auxiliary regression coefficients are also printed out.  See
	Ramanathan, Chapters 7, 8, and 9 for details. In the case of
	White's test, only the squared independent variables are used
	and not their cross products.  In the case of the
	autocorrelation test, if the p-value of the LMF statistic is
	less than 0.05 (and the model was not originally estimated
	with robust standard errors) then serial correlation-robust
	standard errors are calculated and displayed.  For details on
	the calculation of these standard errors see Wooldridge (2002,
	Chapter 12).
      </para>
<para>Menu path: Model window, /Tests</para>
</sect2>

<sect2 id="cmd-logistic" xreflabel="logistic"><title>logistic</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> <literal>[ </literal><literal>ymax=</literal><replaceable>value</replaceable> <literal>] </literal></entry></row>
<row><entry>Examples:</entry><entry><literal>logistic y const x</literal></entry></row>
<row><entry/><entry><literal>logistic y const x ymax=50</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Logistic regression: carries out an OLS regression using the
	logistic transformation of the dependent variable,
	<informalequation><alt role="tex">\[\log\left(\frac{y}{y^*-y}\right)\]</alt><graphic fileref="figures/logistic1"/></informalequation>
	  </para>

<para>The dependent variable must be strictly positive.  If it
	is a decimal fraction, between 0 and 1, the default is to use
	a <emphasis>y<superscript>*</superscript></emphasis> value (the asymptotic
	maximum of the dependent variable) of 1. If the dependent
	variable is a percentage, between 0 and 100, the default
	<emphasis>y<superscript>*</superscript></emphasis> is 100.  If you wish to
	set a different maximum, use the optional
	<literal>ymax=</literal><replaceable>value</replaceable> syntax following
	the list of regressors.  The supplied value must be greater
	than all of the observed values of the dependent
	variable.</para>

<para>The fitted values and residuals from the regression are
	automatically transformed using 	  
	<informalequation><alt role="tex">\[y=\frac{y^*}{1+e^{-x}}\]</alt><graphic fileref="figures/logistic2"/></informalequation> where <emphasis>x</emphasis> represents
	either a fitted value or a residual from the OLS regression
	using the transformed dependent variable.  The reported values
	are therefore comparable with the original dependent
	variable.</para>

<para>Note that if the dependent variable is binary, you should
	use the <command>logit</command> command instead.
      </para>
<para>Menu path: /Model/Logistic</para>
</sect2>

<sect2 id="cmd-logit" xreflabel="logit"><title>logit</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Binomial logit regression. The dependent variable should be a
	binary variable.  Maximum likelihood estimates of the
	coefficients on <replaceable>indepvars</replaceable> are
	obtained via the EM or Expectation&#x2013;Maximization method
	(see Ruud, 2000, Chapter 27).  As the model is nonlinear the
	slopes depend on the values of the independent variables: the
	reported slopes are evaluated at the means of those variables.
	The chi-square statistic tests the null hypothesis that all
	coefficients are zero apart from the constant.
      </para>

<para>
	If you want to use logit for analysis of proportions (where
	the dependent variable is the proportion of cases having a
	certain characteristic, at each observation, rather than a 1
	or 0 variable indicating whether the characteristic is present
	or not) you should not use the <command>logit</command>
	command, but rather construct the logit variable (e.g.
	<command>genr lgt_p = log(p/(1 - p))</command>) and use this
	as the dependent variable in an OLS regression.  See
	Ramanathan, Chapter 12.
      </para>
<para>Menu path: /Model/Logit</para>
</sect2>

<sect2 id="cmd-logs" xreflabel="logs"><title>logs</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	The natural log of each of the variables in
	<replaceable>varlist</replaceable> is obtained and the result stored in a
	new variable with the prefix <literal>l_</literal> which is
	<quote>el</quote> underscore.  <command>logs x y</command> creates the
	new variables <literal>l_x</literal> =
	ln(<literal>x</literal>) and <literal>l_y</literal> =
	ln(<literal>y</literal>).
      </para>
<para>Menu path: /Data/Add variables/logs of selected variables</para>
</sect2>

<sect2 id="cmd-loop" xreflabel="loop"><title>loop</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>control</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>loop 1000</literal></entry></row>
<row><entry/><entry><literal>loop while essdiff &gt; .00001</literal></entry></row>
<row><entry/><entry><literal>loop for i=1991..2000</literal></entry></row>
</tbody></tgroup></informaltable>

<para>The parameter <replaceable>control</replaceable> must take one of three
	forms, as shown in the examples: an integer number of times to
	repeat the commands within the loop;
	<quote><literal>while</literal></quote> plus a numerical
	condition; or <quote><literal>for</literal></quote> plus a
	range of values for the internal index variable
	<literal>i</literal>.
      </para>

<para>This command opens a special mode in which the program
	accepts commands to be executed repeatedly.  Within a loop,
	only certain commands can be used: <command>genr</command>,
	<command>ols</command>, <command>print</command>, <command>sim</command>,
	<command>smpl</command>, <command>store</command> and <command>summary</command>
	(store can't be used in a <quote>while</quote> loop).  You
	exit the mode of entering loop commands with
	<command>endloop</command>: at this point the stacked commands are
	executed.  Loops cannot be nested.
      </para>

<para>See  <xref linkend="looping"/>  for further details and
	examples.</para>

</sect2>

<sect2 id="cmd-meantest" xreflabel="meantest"><title>meantest</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>var1</replaceable> <replaceable>var2</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--unequal-vars</literal> (assume variances are unequal)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Calculates the <emphasis>t</emphasis> statistic for the null
	hypothesis that the population means are equal for the
	variables <replaceable>var1</replaceable> and <replaceable>var2</replaceable>, and shows
	its p-value. By default the statistic is computed on the
	assumption that the variances are equal for the two variables;
	with the <literal>--unequal-vars</literal> flag the variances
	are assumed to be unequal. (The flag will make a difference
	only if there are different numbers of non-missing
	observations for the two variables.)
      </para>
<para>Menu path: /Data/Difference of means</para>
</sect2>

<sect2 id="cmd-modeltab" xreflabel="modeltab"><title>modeltab</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>command</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>Manipulates the gretl <quote>model table</quote>.
	See <xref linkend="modes"/> for details
	<replaceable>command</replaceable> should be one of the following.</para>
<itemizedlist><listitem>
<para><literal>add</literal>: add the last model estimated
	    to the model table, if possible</para>
</listitem>
<listitem>
<para><literal>show</literal>: display the model table in a
	    window</para>
</listitem>
<listitem>
<para><literal>free</literal>: clear the table </para>
</listitem>
</itemizedlist>
<para>Menu path: Session window, Model table icon</para>
</sect2>

<sect2 id="cmd-mpols" xreflabel="mpols"><title>mpols</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Computes OLS estimates with <replaceable>depvar</replaceable> as the
	dependent variable and <replaceable>indepvars</replaceable> as the list of
	independent variables, using multiple precision floating-point
	arithmetic. The variables may be specified by name or number;
	use the number zero for a constant term. This command is
	available only if <application>gretl</application> is compiled with
	support for the Gnu Multiple Precision library (GMP).
      </para>

<para>
	To estimate a polynomial fit, using multiple precision
	arithmetic to generate the required powers of the independent
	variable, use the form, e.g. <command>mpols y 0 x ; 2 3
	  4</command> This does a regression of <literal>y</literal>
	on <literal>x</literal>, <literal>x</literal> squared,
	<literal>x</literal> cubed and <literal>x</literal> to the
	fourth power.  That is, the numbers (which must be positive
	integers) to the right of the semicolon specify the powers of
	<literal>x</literal> to be used.  If more than one independent
	variable is specified, the last variable before the semicolon
	is taken to be the one that should be raised to various
	powers.
      </para>
<para>Menu path: /Model/High precision OLS</para>
</sect2>

<sect2 id="cmd-multiply" xreflabel="multiply"><title>multiply</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>x</replaceable> <replaceable>suffix</replaceable> <replaceable>varlist</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>multiply invpop pc 3 4 5 6</literal></entry></row>
<row><entry/><entry><literal>multiply 1000 big x1 x2 x3</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	The variables in <replaceable>varlist</replaceable>
	(referenced by name or number) are multiplied by
	<replaceable>x</replaceable>, which may be either a numerical
	value or the name of a variable already defined. The products
	are named with the specified <replaceable>suffix</replaceable>
	(maximum 3 characters). The original variable names are
	truncated first if need be. For instance, suppose you want to
	create per capita versions of certain variables, and you have
	the variable <literal>pop</literal> (population).  A suitable
	set of commands is then:</para>
<programlisting>
	genr invpop = 1/pop
	multiply invpop pc income</programlisting>
<para>which will create <literal>incomepc</literal> as the
	product of <literal>income</literal> and
	<literal>invpop</literal>, and <literal>expendpc</literal> as
	<literal>expend</literal> times <literal>invpop</literal>.
      </para>

</sect2>

<sect2 id="cmd-nls" xreflabel="nls"><title>nls</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>function</replaceable> <replaceable>derivatives</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Performs Nonlinear Least Squares (NLS) estimation using a
	modified version of the Levenberg&#x2013;Marquandt algorithm.
	The user must supply a function specification.  The parameters
	of this function must be declared and given starting values
	(using the <command>genr</command> command) prior to
	estimation. Optionally, the user may specify the derivatives
	of the regression function with respect to each of the
	parameters; if analytical derivatives are not supplied, a
	numerical approximation to the Jacobian is computed.</para>

<para>
	It is easiest to show what is required by example.  The
	following is a complete script to estimate the nonlinear
	consumption function set out in William Greene's
	<citetitle>Econometric Analysis</citetitle>, in chapter 11 of
	the 4th edition, or chapter 9 of the 5th.  (The numbers to the
	left of the lines are for reference and are not part of the
	commands.)</para>
<programlisting>
	1   open greene11_3.gdt
	2   ols C 0 Y
	3   genr alpha = coeff(0)
	4   genr beta = coeff(Y)
	5   genr gamma = 1.0
	6   nls C = alpha + beta * Y^gamma
	7   deriv alpha = 1
	8   deriv beta = Y^gamma
	9   deriv gamma = beta * Y^gamma * log(Y)
	10  end nls
      </programlisting>
<para>
	It is often convenient to initialize the parameters by
	reference to a related linear model; that is accomplished here
	on lines 2 to 5.  The parameters alpha, beta and gamma could
	be set to any initial values (not necessarily based on a model
	estimated with OLS), although convergence of the NLS procedure
	is not guaranteed for an arbitrary starting point.</para>

<para>
	The actual NLS commands occupy lines 6 to 10. On line 6 the
	<command>nls</command> command is given: a dependent variable
	is specified, followed by an equals sign, followed by a
	function specification.  The syntax for the expression on the
	right is the same as that for the <command>genr</command>
	command.  The next three lines specify the derivatives of the
	regression function with respect to each of the parameters in
	turn.  Each line begins with the keyword
	<command>deriv</command>, gives the name of a parameter, an
	equals sign, and an expression whereby the derivative can be
	calculated (again, the syntax here is the same as for
	<command>genr</command>). These <command>deriv</command> lines
	are optional, but it is recommended that you supply them if
	possible. Line 10, <command>end nls</command>, completes the
	command and calls for estimation.</para>

<para>For further details on NLS estimation please see
	 <xref linkend="nlschap"/> .</para>
<para>Menu path: /Model/Nonlinear Least Squares</para>
</sect2>

<sect2 id="cmd-nulldata" xreflabel="nulldata"><title>nulldata</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>series_length</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>nulldata 500</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Establishes a <quote>blank</quote> data set, containing only a
	constant and an index variable, with periodicity 1 and the
	specified number of observations. This may be used for
	simulation purposes: some of the <command>genr</command> commands
	(e.g. <command>genr uniform()</command>, <command>genr normal()</command>)
	will generate dummy data from scratch to fill out the data
	set. This command may be useful in conjunction with
	<command>loop</command>.  See also the <quote>seed</quote> option to
	the <command>set</command> command.
      </para>
<para>Menu path: /File/Create data set</para>
</sect2>

<sect2 id="cmd-ols" xreflabel="ols"><title>ols</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
<row><entry/><entry><literal>--robust</literal> (robust standard errors)</entry></row>
<row><entry/><entry><literal>--quiet</literal> (suppress printing of results)</entry></row>
<row><entry>Examples:</entry><entry><literal>ols 1 0 2 4 6 7</literal></entry></row>
<row><entry/><entry><literal>ols y 0 x1 x2 x3 --vcv</literal></entry></row>
<row><entry/><entry><literal>ols y 0 x1 x2 x3 --quiet</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
        Computes ordinary least squares (OLS) estimates with
        <replaceable>depvar</replaceable> as the dependent variable and
        <replaceable>indepvars</replaceable> as the list of independent variables.
      </para>

<para>Variables may be specified by name or number; use the
        number zero for a constant term. The program also prints the
        p-values for <emphasis>t</emphasis> (two-tailed) and
        <emphasis>F</emphasis>-statistics.  A p-value below 0.01
        indicates significance at the 1 percent level and is denoted
        by <literal>***</literal>. <literal>**</literal> indicates
        significance between 1 and 5 percent and <literal>*</literal>
        indicates significance between 5 and 10 percent levels. Model
        selection statistics (described in Ramanathan, Section 4.3)
        are also printed. Various internal variables may be retrieved
        using the <command>genr</command> command, provided
        <command>genr</command> is invoked immediately after this
        command.
      </para>
<para>Menu path: /Model/Ordinary Least Squares</para><para>Other access: Beta-hat button on toolbar</para>
</sect2>

<sect2 id="cmd-omit" xreflabel="omit"><title>omit</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
<row><entry>Example:</entry><entry><literal>omit 5 7 9</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	This command must be invoked after an estimation command. The
	variables in <replaceable>varlist</replaceable> are omitted
	from the previous model and the new model estimated. If more
	than one variable is omitted, the Wald
	<emphasis>F</emphasis>-statistic for the omitted variables
	will be printed along with its p-value (for the OLS procedure
	only).  A p-value below 0.05 means that the coefficients are
	jointly significant at the 5 percent level.
      </para>
<para>Menu path: Model window, /Tests/omit variables</para>
</sect2>

<sect2 id="cmd-omitfrom" xreflabel="omitfrom"><title>omitfrom</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>modelID</replaceable> <replaceable>varlist</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>omitfrom 2 5 7 9</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Works like the <command>omit</command> command, except
	that you specify a previous model (using its ID number, which
	is printed at the start of the model output) to take as the
	base for omitting variables.  The example above omits
	variables number 5, 7 and 9 from Model 2.
      </para>
<para>Menu path: Model window, /Tests/omit variables</para>
</sect2>

<sect2 id="cmd-open" xreflabel="open"><title>open</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>datafile</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Opens a data file.  If a data file is already open, it is
	replaced by the newly opened one.  The program will try to
	detect the format of the data file (native, CSV or BOX1).
      </para>

<para>
	This command can also be used to open a database (gretl or
	RATS 4.0) for reading, in which case it should be followed by
	the <xref linkend="cmd-data"/> command to extract particular
	series from the database.
      </para>
<para>Menu path: /File/Open data</para><para>Other access: Drag a data file into gretl (MS Windows or
	Gnome)</para>
</sect2>

<sect2 id="cmd-outfile" xreflabel="outfile"><title>outfile</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>filename</replaceable> <replaceable>option</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--append</literal> (append to file)</entry></row>
<row><entry/><entry><literal>--close</literal> (close file)</entry></row>
<row><entry/><entry><literal>--write</literal> (overwrite file)</entry></row>
<row><entry>Examples:</entry><entry><literal>outfile --write regress.txt</literal></entry></row>
<row><entry/><entry><literal>outfile --close</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Diverts output to <replaceable>filename</replaceable>, until further
	notice.  Use the flag <literal>--append</literal> to append
	output to an existing file, or <literal>--write</literal> to
	start a new file (or overwrite an existing one).  Only one
	file can be opened in this way at any given time.</para>

<para>The <literal>--close</literal> flag is used to close an
	output file that was previously opened as above.  Output will
	then revert to the default stream.</para>

<para>In the first example above, the file
	<filename>regress.txt</filename> is opened for writing, and in
	the second example it is closed.  This would make sense as a
	sequence only if some commands were issued before the
	<literal>--close</literal>.  For example if an <command>ols</command>
	command intervened, its output would go to
	<filename>regress.txt</filename> rather than the
	screen.</para>

</sect2>

<sect2 id="cmd-panel" xreflabel="panel"><title>panel</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Options:</entry><entry><literal>--cross-section</literal> (stacked cross sections)</entry></row>
<row><entry/><entry><literal>--time-series</literal> (stacked time series)</entry></row>
</tbody></tgroup></informaltable>

<para>Request that the current data set be interpreted as a
	panel (pooled cross section and time series).  By default, or
	with the <literal>--time-series</literal> flag, the data set
	is taken to be in the form of stacked time series (successive
	blocks of data contain time series for each cross-sectional
	unit).  With the <literal>--cross-section</literal> flag, the
	data set is read as stacked cross-sections (successive blocks
	contain cross sections for each time period).  See also
	<xref linkend="cmd-setobs"/>.
      </para>
<para>Menu path: /Sample/interpret as panel</para>
</sect2>

<sect2 id="cmd-pca" xreflabel="pca"><title>pca</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--save-all</literal> (Save all components)</entry></row>
<row><entry/><entry><literal>--save</literal> (Save major components)</entry></row>
</tbody></tgroup></informaltable>

<para>Rudimentary Principal Components Analysis.  Prints the
	eigenvalues of the correlation matrix for the variables in
	<replaceable>varlist</replaceable> along with the proportion of the joint
	variance accounted for by each component.  Also prints the
	corresponding eigenvectors (<quote>component
	  loadings</quote>).</para>

<para>If the <literal>--save</literal> flag is given, components
	with eigenvalues greater than 1.0 are saved to the dataset as
	variables, with names <literal>PC1</literal>,
	<literal>PC2</literal> and so on.  These artificial variables
	are formed as the sum of (component loading) times
	(standardized <literal>Xi</literal>), where
	<literal>Xi</literal> denotes the <emphasis>i</emphasis>th
	variable in <replaceable>varlist</replaceable>.</para>

<para>If the <literal>--save-all</literal> flag is given, all of
	the components are saved as described above.</para>
<para>Menu path: Main window pop-up (multiple selection)</para>
</sect2>

<sect2 id="cmd-pergm" xreflabel="pergm"><title>pergm</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varname</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--bartlett</literal> (use Bartlett lag window)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Computes and displays (and if not in batch mode, graphs) the
	spectrum of the specified variable.  Without the
	<literal>--bartlett</literal> flag the sample periodogram is
	given; with the flag a Bartlett lag window of length 2 root
	<emphasis>T</emphasis> (where <emphasis>T</emphasis> is the sample
	size) is used in estimating the spectrum (see Chapter 18 of
	Greene's <citetitle>Econometric Analysis</citetitle>). When the sample
	periodogram is printed, a <emphasis>t</emphasis>-test for
	fractional integration of the series (<quote>long
	  memory</quote>) is also given: the null hypothesis is that
	the integration order is zero.
      </para>
<para>Menu path: /Variable/spectrum</para><para>Other access: Main window pop-up menu (single
	selection)</para>
</sect2>

<sect2 id="cmd-plot" xreflabel="plot"><title>plot</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varlist</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--one-scale</literal> (force a single scale)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Plots the values for specified variables, for the range of
	observations currently in effect, using ASCII symbols.  Each
	line stands for an observation and the values are plotted
	horizontally.  By default the variables are scaled
	appropriately.  See also <xref linkend="cmd-gnuplot"/>.
      </para>

</sect2>

<sect2 id="cmd-pooled" xreflabel="pooled"><title>pooled</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>Estimates a model via OLS (see <xref linkend="cmd-ols"/> for
	details on syntax), and flags it as a pooled or panel model,
	so that the <xref linkend="cmd-hausman"/> test item becomes
	available.</para>
<para>Menu path: /Model/Pooled OLS</para>
</sect2>

<sect2 id="cmd-print" xreflabel="print"><title>print</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>varlist</replaceable>  or <replaceable>string_literal</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--byobs</literal> (by observations)</entry></row>
<row><entry/><entry><literal>--ten</literal> (use 10 significant digits)</entry></row>
<row><entry>Examples:</entry><entry><literal>print x1 x2 --byobs</literal></entry></row>
<row><entry/><entry><literal>print "This is a string"</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	If <replaceable>varlist</replaceable> is given, prints the values of the
	specified variables; if no list is given, prints the values of
	all variables in the current data file. If the
	<literal>--byobs</literal> flag is given the data are printed
	by observation, otherwise they are printed by variable.  If
	the <literal>--ten</literal> flag is given the data are
	printed by variable to 10 significant digits.
      </para>

<para>
	If the argument to <command>print</command> is a literal string (which
	must start with a double-quote, <literal>"</literal>), the
	string is printed as is.  See also <xref linkend="cmd-printf"/>.</para>
<para>Menu path: /Data/Display values</para>
</sect2>

<sect2 id="cmd-printf" xreflabel="printf"><title>printf</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>format</replaceable> <replaceable>args</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Prints scalar values under the control of a format string
	(providing a small subset of the <literal>printf()</literal>
	statement in the C programming language). Recognized formats
	are <literal>%g</literal> and <literal>%f</literal>, in each
	case with the various modifiers available in C.  Examples: the
	format <literal>%.10g</literal> prints a value to 10
	significant figures; <literal>%12.6f</literal> prints a value
	to 6 decimal places, with a width of 12 characters.</para>

<para>The format string itself must be enclosed in double
	quotes.  The values to be printed must follow the format
	string, separated by commas.  These values should take the
	form of either (a) the names of variables in the dataset, or
	(b) expressions that are valid for the <command>genr</command>
	command.  The following example prints the values of two
	variables plus that of a calculated expression:</para>
<programlisting>
	ols 1 0 2 3
	genr b = coeff(2)
	genr se_b = stderr(2)
	printf "b = %.8g, standard error %.8g, t = %.4f\n", b, se_b, b/se_b
      </programlisting>
<para>
	The maximum length of a format string is 127 characters.  The
	escape sequences <literal>\n</literal> (newline),
	<literal>\t</literal> (tab), <literal>\v</literal> (vertical
	tab) and <literal>\\</literal> (literal backslash) are
	recognized.  To print a literal percent sign, use
	<literal>%%</literal>.</para>

</sect2>

<sect2 id="cmd-probit" xreflabel="probit"><title>probit</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>depvar</replaceable> <replaceable>indepvars</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--vcv</literal> (print covariance matrix)</entry></row>
</tbody></tgroup></informaltable>

<para>
	Probit regression. The dependent variable should be a binary
	variable. Maximum likelihood estimates of the coefficients on
	<replaceable>indepvars</replaceable> are obtained via iterated
	least squares (the EM or Expectation&#x2013;Maximization
	method).  As the model is nonlinear the slopes depend on the
	values of the independent variables: the reported slopes are
	evaluated at the means of those variables.  The chi-square
	statistic tests the null hypothesis that all coefficients are
	zero apart from the constant.
      </para>

<para>
	Probit for analysis of proportions is not implemented in
	<application>gretl</application> at this point.
      </para>
<para>Menu path: /Model/Probit</para>
</sect2>

<sect2 id="cmd-pvalue" xreflabel="pvalue"><title>pvalue</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>dist</replaceable> <literal>[ </literal><replaceable>params</replaceable> <literal>] </literal><replaceable>xval</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>pvalue z zscore</literal></entry></row>
<row><entry/><entry><literal>pvalue t 25 3.0</literal></entry></row>
<row><entry/><entry><literal>pvalue X 3 5.6</literal></entry></row>
<row><entry/><entry><literal>pvalue F 4 58 fval</literal></entry></row>
<row><entry/><entry><literal>pvalue G xbar varx x</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Computes the area to the right of <replaceable>xval</replaceable> in the
	specified distribution (<literal>z</literal> for Gaussian,
	<literal>t</literal> for Student's <emphasis>t</emphasis>,
	<literal>X</literal> for chi-square, <literal>F</literal> for
	<emphasis>F</emphasis> and <literal>G</literal> for gamma).  For
	the <emphasis>t</emphasis> and chi-square distributions the
	degrees of freedom must be given; for <emphasis>F</emphasis>
	numerator and denominator degrees of freedom are required; and
	for gamma the mean and variance are needed.
      </para>
<para>Menu path: /Utilities/p-value finder</para>
</sect2>

<sect2 id="cmd-quit" xreflabel="quit"><title>quit</title>

<para>
	Exits from the program, giving you the option of saving the
	output from the session on the way out.  
      </para>
<para>Menu path: /File/Exit</para>
</sect2>

<sect2 id="cmd-rename" xreflabel="rename"><title>rename</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>varnumber</replaceable> <replaceable>newname</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>Changes the name of the variable with identification
	number <replaceable>varnumber</replaceable> to <replaceable>newname</replaceable>.  The
	<replaceable>varnumber</replaceable> must be between 1 and the number of
	variables in the dataset.  The new name must be of 8
	characters maximum, must start with a letter, and must be
	composed of only letters, digits, and the underscore
	character.
      </para>
<para>Menu path: /Variable/Edit attributes</para><para>Other access: Main window pop-up menu (single selection)</para>
</sect2>

<sect2 id="cmd-reset" xreflabel="reset"><title>reset</title>

<para>
	Must immediately follow the estimation of a model via OLS.
	Carries out Ramsey's RESET test for model specification
	(non-linearity) by adding the square and the cube of the
	fitted values to the regression and calculating the
	<emphasis>F</emphasis> statistic for the null hypothesis that
	the parameters on the two added terms are zero.
      </para>
<para>Menu path: Model window, /Tests/Ramsey's RESET</para>
</sect2>

<sect2 id="cmd-rhodiff" xreflabel="rhodiff"><title>rhodiff</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>rholist</replaceable> ; <replaceable>varlist</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>rhodiff .65 ; 2 3 4</literal></entry></row>
<row><entry/><entry><literal>rhodiff r1 r2 ; x1 x2 x3</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Creates rho-differenced counterparts of the variables (given
	by number or by name) in <replaceable>varlist</replaceable>
	and adds them to the data set, using the suffix
	<literal>#</literal> for the new variables. Given variable
	<literal>v1</literal> in <replaceable>varlist</replaceable>,
	and entries <literal>r1</literal> and <literal>r2</literal> in
	<replaceable>rholist</replaceable>, <literal>v1# = v1(t) -
	  r1*v1(t-1) - r2*v1(t-2)</literal> is created. The
	<replaceable>rholist</replaceable> entries can be given as
	numerical values or as the names of variables previously
	defined.
      </para>

</sect2>

<sect2 id="cmd-rmplot" xreflabel="rmplot"><title>rmplot</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varname</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Range&#x2013;mean plot: this command creates a simple graph to
	help in deciding whether a time series,
	<emphasis>y</emphasis>(t), has constant variance or not.  We
	take the full sample t=1,...,T and divide it into small
	subsamples of arbitrary size <emphasis>k</emphasis>. The first
	subsample is formed by
	<emphasis>y</emphasis>(1),...,<emphasis>y</emphasis>(k), the
	second is <emphasis>y</emphasis>(k+1), ...,
	<emphasis>y</emphasis>(2k), and so on.  For each subsample we
	calculate the sample mean and range (= maximum minus minimum),
	and we construct a graph with the means on the horizontal axis
	and the ranges on the vertical. So each subsample is
	represented by a point in this plane.  If the variance of the
	series is constant we would expect the subsample range to be
	independent of the subsample mean; if we see the points
	approximate an upward-sloping line this suggests the variance
	of the series is increasing in its mean; and if the points
	approximate a downward sloping line this suggests the variance
	is decreasing in the mean.</para>

<para>Besides the graph, gretl displays the means and ranges for
	each subsample, along with the slope coefficient for an OLS
	regression of the range on the mean and the p-value for the
	null hypothesis that this slope is zero.  If the slope
	coefficient is significant at the 10 percent significance
	level then the fitted line from the regression of range on
	mean is shown on the graph.
      </para>
<para>Menu path: /Variable/Range-mean graph</para>
</sect2>

<sect2 id="cmd-run" xreflabel="run"><title>run</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>inputfile</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Execute the commands in <replaceable>inputfile</replaceable> then return
	control to the interactive prompt.
      </para>
<para>Menu path: Run icon in script window</para>
</sect2>

<sect2 id="cmd-runs" xreflabel="runs"><title>runs</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Argument:</entry><entry><replaceable>varname</replaceable> </entry></row>
</tbody></tgroup></informaltable>

<para>
	Carries out the nonparametric <quote>runs</quote> test for
	randomness of the specified variable.  If you want to test for
	randomness of deviations from the median, for a variable named
	<literal>x1</literal> with a non-zero median, you can do the
	following:</para>
<programlisting>
	genr signx1 = x1 - median(x1)
	runs signx1
      </programlisting><para>Menu path: /Variable/Runs test</para>
</sect2>

<sect2 id="cmd-scatters" xreflabel="scatters"><title>scatters</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>yvar</replaceable> ; <replaceable>xvarlist</replaceable>  or <replaceable>yvarlist ; xvar</replaceable> </entry></row>
<row><entry>Examples:</entry><entry><literal>scatters 1 ; 2 3 4 5</literal></entry></row>
<row><entry/><entry><literal>scatters 1 2 3 4 5 6 ; 7</literal></entry></row>
</tbody></tgroup></informaltable>

<para>
	Plots pairwise scatters of <replaceable>yvar</replaceable> against all the
	variables in <replaceable>xvarlist</replaceable>, or of all the variables in
	<replaceable>yvarlist</replaceable> against <replaceable>xvar</replaceable>.  The first
	example above puts variable 1 on the <emphasis>y</emphasis>-axis
	and draws four graphs, the first having variable 2 on the
	<emphasis>x</emphasis>-axis, the second variable 3 on the
	<emphasis>x</emphasis>-axis, and so on.  The second example
	plots each of variables 1 through 6 against variable 7 on the
	<emphasis>x</emphasis>-axis. Scanning a set of such plots can be
	a useful step in exploratory data analysis.  The maximum
	number of plots is six; any extra variable in the list will be
	ignored.
      </para>
<para>Menu path: /Data/Multiple scatterplots</para>
</sect2>

<sect2 id="cmd-set" xreflabel="set"><title>set</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-setobs" xreflabel="setobs"><title>setobs</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-setmiss" xreflabel="setmiss"><title>setmiss</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-shell" xreflabel="shell"><title>shell</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-sim" xreflabel="sim"><title>sim</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-smpl" xreflabel="smpl"><title>smpl</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--dummy</literal> (.)</entry></row>
<row><entry/><entry><literal>--no-missing</literal> (.)</entry></row>
<row><entry/><entry><literal>--restrict</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-spearman" xreflabel="spearman"><title>spearman</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--verbose</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-square" xreflabel="square"><title>square</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--cross</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-store" xreflabel="store"><title>store</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Options:</entry><entry><literal>--csv</literal> (.)</entry></row>
<row><entry/><entry><literal>--gnu-octave</literal> (.)</entry></row>
<row><entry/><entry><literal>--gnu-R</literal> (.)</entry></row>
<row><entry/><entry><literal>--traditional</literal> (.)</entry></row>
<row><entry/><entry><literal>--gzipped</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-summary" xreflabel="summary"><title>summary</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-system" xreflabel="system"><title>system</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-tabprint" xreflabel="tabprint"><title>tabprint</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--complete</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-testuhat" xreflabel="testuhat"><title>testuhat</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-tobit" xreflabel="tobit"><title>tobit</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--verbose</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-tsls" xreflabel="tsls"><title>tsls</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-var" xreflabel="var"><title>var</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Option:</entry><entry><literal>--quiet</literal> (.)</entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-varlist" xreflabel="varlist"><title>varlist</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-vartest" xreflabel="vartest"><title>vartest</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

<sect2 id="cmd-wls" xreflabel="wls"><title>wls</title>

<informaltable role="cmd" frame="none"><tgroup cols="2"><colspec colnum="1" colwidth="82pt"/><tbody>
<row><entry>Arguments:</entry><entry><replaceable>.</replaceable> <replaceable>.</replaceable> </entry></row>
<row><entry>Example:</entry><entry><literal>.</literal></entry></row>
</tbody></tgroup></informaltable>

<para>Description goes here.</para>
<para>Menu path: .</para><para>Other access: .</para>
</sect2>

