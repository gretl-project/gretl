  <?PSGML NOFILL programlisting example informalequation?>

  <chapter id="c-cli">
    <title>El interfaz de línea de instrucciones</title>

    <sect1 id="cli-console">
      <title><application>Gretl</application> en la consola</title>

      <para>El paquete <application>gretl</application> incluye el programa
      de línea de instrucciones <application>gretlcli</application>. Este
      es, esencialmente, una versión actualizada del programa ESL de Ramu
      Ramanathan. En Linux, puede ejecutarse desde la consola, o en un xterm (o
      similar). En MS Windows, puede ejecutarse desde una <quote>ventana de
      MSDOS</quote>. <application>Gretlcli</application> cuenta con su propio
      archivo de ayuda, al que puede accederse tecleando la palabra
      <quote>help</quote> desde el cursor. Es posible ejecutarlo en un
      proceso por lotes, y enviar los resultados directamente a un archivo (véase
      <xref linkend="optarg2" /> arriba).</para>

      <para>Si <application>gretlcli</application> está conectado a la
      biblioteca <application><quote>readline</quote></application> (esto ocurre
      automáticamente en el caso de la versión MS Windows; véase también el <xref
      linkend="app-technote" />), la línea de instrucciones es recuperable y
      editable, y ofrece terminación de las instrucciones. Podemos utilizar las
      flechas de Arriba y Abajo para pasar a las instrucciones que ya han sido
      tecleados previamente. En cualquier línea de instrucciones, podemos utilizar
      las flechas para movernos, junto con las combinaciones de teclas para
      editar de Emacs.<footnote><para>En realidad, las combinaciones dadas a
      continuación son sólo laos que hay por defecto; pueden ser
      personalizadas. Véase el <ulink
      url="http://cnswww.cns.cwru.edu/~chet/readline/readline.html">manual de readline
      </ulink>.</para></footnote> Las más comunes son :</para>

      <informaltable frame="none">
        <tgroup cols="2">
          <thead>
            <row>
              <entry>Combinaciones de teclas</entry>

              <entry>Efecto</entry>
            </row>
          </thead>

          <tbody>
            <row>
              <entry><literal>Ctrl-a</literal></entry>

              <entry>ir al inicio de la línea</entry>
            </row>

            <row>
              <entry><literal>Ctrl-e</literal></entry>

              <entry>ir al final de la línea</entry>
            </row>

            <row>
              <entry><literal>Ctrl-d</literal></entry>

              <entry>eliminar el carácter de la derecha</entry>
            </row>
          </tbody>
        </tgroup>
      </informaltable>

      <para>donde <quote><literal>Ctrl-a</literal></quote> significa pulsar la
      tecla <quote><literal>a</literal></quote> a la vez que la tecla
      <quote><literal>Ctrl</literal></quote>. Así, si queremos cambiar algo en
      el inicio de una instrucción, <emphasis>no</emphasis> es preciso ir hacia
      atrás por toda la línea, borrándola con nuestro paso. Simplemente
      saltamos al inicio y añadimos o eliminamos caracteres.</para>

      <para>Al teclear las primeras letras de una instrucción, pulsando la tecla
      Tab el programa <application>readline</application> intentará completar el
      nombre de la instrucción. Si es una combinación única, saldrá la instrucción
      automáticamente. Si hay más de una posibilidad, pulsando Tab otra vez,
      se mostrará una lista.</para>
    </sect1>

    <sect1 id="cli-syntax">
      <title>Diferencias con ESL</title>

      <para>Los lotes o guiones de instrucciones desarrollados para el programa <application>ESL</application>
      original de Ramanathan deberían de ser utilizables en <application>gretlcli</application>
      con pocos o ningún cambio: en lo único que hay que tener cuidado es en
      las instrucciones multilineales y la instrucción <command>freq</command>.</para>

      <itemizedlist>
        <listitem>
          <para><application>ESL</application> utiliza el punto y coma como
          terminación de muchas de las instrucciones. Esto ha sido eliminado en
          <application>gretlcli</application>. El punto y coma simplemente se
          ignora, excepto en unos casos especiales donde tiene un significado
          determinante: como separador de dos listas en las instrucciones
          <command>ar</command> y <command>tsls</command>, o como marcador
          para una primera o última observación sin cambios en la instrucción
          <command>smpl</command>. En <application>ESL</application>, el punto y coma da la
          posibilidad de partir una instrucción larga de más de una linea;
          en <application>gretlcli</application> esto se hace con un
          <literal>\</literal> situado al final de la línea que va a
          continuarse.</para>
        </listitem>

        <listitem>
          <para>Con <command>freq</command>, actualmente no se
          pueden especificar rangos definidos por el usuario como en
          <application>ESL</application>. Un contraste chi-cuadrado para
          normalidad ha sido añadido a los resultados de esta instrucción.</para>
        </listitem>
      </itemizedlist>

      <para>Nótese que se ha simplificado la sintaxis de línea de instrucciones
      para un proceso por lotes. En <application>ESL</application> se tecleaba,
      por ejemplo
      <programlisting>
        esl -b datafile &lt; inputfile &gt; outputfile
      </programlisting> mientras que en <application>gretlcli</application> hay
      que teclear: <programlisting>
    gretlcli -b inputfile &gt; outputfile
      </programlisting>
      El archivo de entrada se trata como un
      argumento del programa; debe de especificar el archivo de datos que hay
      que usar de forma interna, utilizando la sintaxis <command>open fichero_de_datos</command>
      o el comentario especial <literal>(* !</literal>
      <replaceable>datafile</replaceable> <literal>*)</literal></para>
    </sect1>
  </chapter>

  <!-- Keep this comment at the end of the file
Local variables:
sgml-default-dtd-file:"../manual.ced"
mode: xml
sgml-parent-document:("../manual.xml" "book" "chapter")
End:
-->

