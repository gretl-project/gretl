<?xml version="1.0" encoding="iso-8859-1"?>

<?PSGML NOFILL programlisting example informalequation?>

  <chapter id="vecm-explanation"><title>Cointegrazione e modelli Vector Error
    Correction</title>

  <sect1 id="johansen-test"><title>Il test di cointegrazione di Johansen</title>

    <para>
      Il test di Johansen per la cointegrazione deve tenere conto di quali
      ipotesi vengono fatte a proposito dei termini deterministici, per cui
      si possono individuare i ben noti <quote>cinque casi</quote>. Una
      presentazione approfondita dei cinque casi richiede una certa quantità di
      algebra matriciale, ma è possibile dare un'intuizione del problema per
      mezzo di un semplice esempio.
    </para>

    <para>
      Si consideri una serie &xt; che si comporta nel modo seguente
      <equation id="vecm1"><alt role="tex">
	  \[ x_t = m + x_{t-1} + \varepsilon_t \]
	</alt><graphic fileref="figures/vecm1"/>
      </equation> dove <mathvar>m</mathvar> è un numero reale e
      &varepsilon;<mathvar><subscript>t</subscript></mathvar> è un processo
      <quote>white noise</quote>.  Come si può facilmente mostrare, &xt; è un
      <quote>random walk</quote> che fluttua intorno a un trend deterministico
      con pendenza <mathvar>m</mathvar>. Nel caso particolare in cui
      <mathvar>m</mathvar> = 0, il trend deterministico scompare e
      &xt; è un puro random walk.
    </para>

    <para>
      Si consideri ora un altro processo &yt;, definito da
      <informalequation><alt role="tex">
	  \[ y_t = k + x_t + u_t \]
	</alt><graphic fileref="figures/vecm2"/>
      </informalequation> dove, ancora, <mathvar>k</mathvar> è un numero reale e
      &ut; è un processo white noise. Poiché &ut; è stazionario per definizione,
      &xt; e &yt; sono cointegrate, ossia la loro differenza
      <inlineequation><alt role="tex">
	  $z_t = y_t - x_t = k + u_t$
	</alt><graphic fileref="figures/vecm2a"/>
      </inlineequation>
      è un processo stazionario. Per <mathvar>k</mathvar> = 0, &zt; è un semplice
      white noise a media zero, mentre per <mathvar>k</mathvar> &ne; 0 il processo
      &zt; è white noise con media diversa da zero.
  </para>

    <para>
      Dopo alcune semplici sostituzioni, le due equazioni precedenti possono essere
      rappresentate congiuntamente come un sistema VAR(1)
      <informalequation><alt role="tex">
\renewcommand{\arraystretch}{1.3}
\setlength{\arraycolsep}{.05in}
	\[ \left[  \begin{array}{c} y_t \\ x_t \end{array} \right] = \left[
	\begin{array}{c} k + m \\ m \end{array} \right] + \left[
	\begin{array}{rr} 0 &amp; 1 \\ 0 &amp; 1 \end{array} \right] \left[
	\begin{array}{c} y_{t-1} \\ x_{t-1} \end{array} \right] + \left[
	\begin{array}{c} u_t + \varepsilon_t \\ \varepsilon_t \end{array}
	\right] \]
	</alt><graphic fileref="figures/vecm3"/>
      </informalequation>
      o in forma VECM
      <informalequation><alt role="tex">
\renewcommand{\arraystretch}{1.3}
\setlength{\arraycolsep}{.05in}
\begin{eqnarray*}
  \left[  \begin{array}{c} \Delta y_t \\ \Delta x_t \end{array} \right]  &amp; = &amp; 
    \left[  \begin{array}{c} k + m \\ m \end{array} \right] +
    \left[  \begin{array}{rr} -1 &amp; 1 \\ 0 &amp; 0 \end{array} \right] 
    \left[  \begin{array}{c} y_{t-1} \\ x_{t-1} \end{array} \right] + 
    \left[  \begin{array}{c} u_t + \varepsilon_t \\ \varepsilon_t \end{array} \right] = \\
  &amp; = &amp; 
    \left[  \begin{array}{c} k + m \\ m \end{array} \right] +
    \left[  \begin{array}{r} -1 \\ 0 \end{array} \right]
    \left[  \begin{array}{rr} 1 &amp; -1 \end{array} \right] 
    \left[  \begin{array}{c} y_{t-1} \\ x_{t-1} \end{array} \right] + 
    \left[  \begin{array}{c} u_t + \varepsilon_t \\ \varepsilon_t \end{array} \right] = \\
  &amp; = &amp;
    \mu_0 + \alpha \beta^' \left[  \begin{array}{c} y_{t-1} \\ x_{t-1} \end{array} \right] + \eta_t = 
    \mu_0 + \alpha z_{t-1} + \eta_t ,
\end{eqnarray*}
	</alt><graphic fileref="figures/vecm4"/>
      </informalequation>
      dove &beta; è il vettore di cointegrazione e &alpha; è il vettore
      dei <quote>loading</quote> o <quote>aggiustamenti</quote>.
    </para>

    <para> 
      Possiamo ora considerare tre casi possibili:
    </para>

    <orderedlist>
      <listitem>
	<para>
	  <mathvar>m</mathvar> &ne; 0: In questo caso &xt; ha un trend, come
          abbiamo appena visto; ne consegue che anche &yt; segue un trend lineare
          perché in media si mantiene a una distanza di <mathvar>k</mathvar> da &xt;.
          Il vettore &mu;<subscript>0</subscript> non ha restrizioni. Questo è
          il caso predefinito per il comando <xref linkend="cmd-vecm"/> di
          gretl.
	</para>
      </listitem>
      <listitem>
	<para>
          <mathvar>m</mathvar> = 0 e <mathvar>k</mathvar> &ne; 0: In questo
          caso, &xt; non ha un trend, e di conseguenza neanche &yt;.  Tuttavia,
          la distanza media tra &yt; e &xt; è diversa da zero. Il vettore
          &mu;<subscript>0</subscript> è dato da
	  <informalequation><alt role="tex">
\renewcommand{\arraystretch}{1.3}
\setlength{\arraycolsep}{.05in}
	      \[
	      \mu_0 = \left[  \begin{array}{c} k \\ 0 \end{array} \right]
	      \]
	    </alt><graphic fileref="figures/vecm5"/>
	  </informalequation>
	  che è non nullo, quindi il VECM mostrato sopra ha un termine costante.
	  La costante, tuttavia è soggetta alla restrizione che il suo secondo
          elemento deve essere pari a 0. Più in generale,
          &mu;<subscript>0</subscript> è un multiplo del vettore &alpha;. Si
          noti che il VECM potrebbe essere scritto anche come
	  <informalequation><alt role="tex">
\renewcommand{\arraystretch}{1.3}
\setlength{\arraycolsep}{.05in}
   \[
     \left[  \begin{array}{c} \Delta y_t \\ \Delta x_t \end{array} \right]  =  
     \left[  \begin{array}{r} -1 \\ 0 \end{array} \right]
     \left[  \begin{array}{rrr} 1 &amp; -1 &amp; -k \end{array} \right] 
     \left[  \begin{array}{c} y_{t-1} \\ x_{t-1} \\ 1 \end{array} \right] + 
     \left[  \begin{array}{c} u_t + \varepsilon_t \\ \varepsilon_t  \end{array} \right]
   \]
	   </alt><graphic fileref="figures/vecm6"/>   
	  </informalequation>
	  che incorpora l'intercetta nel vettore di cointegrazione. Questo è il
          caso di <quote>costante vincolata</quote>; può essere specificato nel
          comando <xref linkend="cmd-vecm"/> di gretl usando l'opzione
          <literal>--rc</literal>.
	</para>
      </listitem>
      <listitem>
	<para>
	  <mathvar>m</mathvar> = 0 e <mathvar>k</mathvar> = 0: Questo caso è il
          più vincolante: chiaramente, né &xt; né &yt; hanno un trend, e la loro
          distanza media è zero. Anche il vettore &mu;<subscript>0</subscript>
          vale 0, quindi questo caso può essere chiamato <quote>senza costante</quote>.
          Questo caso è specificato usando l'opzione <literal>--nc</literal> con
	  <xref linkend="cmd-vecm"/>.
	</para>
      </listitem>
    </orderedlist>

    <para>
      Nella maggior parte dei casi, la scelta tra le tre possibilità si basa su
      un misto di osservazione empirica e di ragionamento economico. Se le
      variabili in esame sembrano seguire un trend lineare, è opportuno non
      imporre alcun vincolo all'intercetta. Altrimenti occorre chiedersi se ha
      senso specificare una relazione di cointegrazione che includa
      un'intercetta diversa da zero. Un esempio appropriato potrebbe essere la
      relazione tra due tassi di interesse: in generale questi non hanno un
      trend, ma il VAR potrebbe comunque avere un'intercetta perché la
      differenza tra i due (lo <quote>spread</quote> sui tassi d'interesse)
      potrebbe essere stazionaria attorno a una media diversa da zero (ad
      esempio per un premio di liquidità o di rischio).
    </para>

    <para>
      L'esempio precedente può essere generalizzato in tre direzioni:
    </para>

    <orderedlist>
      <listitem>
	<para>
          Se si considera un VAR di ordine maggiore di 1, l'algebra si complica,
          ma le conclusioni sono identiche.
        </para>
      </listitem>
      <listitem>
	<para>
	  Se il VAR include più di due variabili endogene, il rango di
          cointegrazione <mathvar>r</mathvar> può essere maggiore di 1. In
          questo caso &alpha; è una matrice con <mathvar>r</mathvar> colonne e
          il caso con la costante vincolata comporta che
          &mu;<subscript>0</subscript> sia una combinazione lineare delle
          colonne di &alpha;.
	</para>
      </listitem>
      <listitem>
	<para>
          Se si include un trend lineare nel modello, la parte deterministica
          del VAR diventa &mu;<subscript>0</subscript> +
          &mu;<subscript>1</subscript><mathvar>t</mathvar>. Il ragionamento è
          praticamente quello visto sopra, tranne per il fatto che l'attenzione
          è ora posta su &mu;<subscript>1</subscript> invece che su
          &mu;<subscript>0</subscript>. La controparte del caso con
	  <quote>costante vincolata</quote> discusso sopra è un caso con
          <quote>trend vincolato</quote>, così che le relazioni di
          cointegrazione includono un trend, ma la differenza prima delle
          variabili in questione no. Nel caso di un trend non vincolato, il
          trend appare sia nelle relazioni di cointegrazione sia nelle
          differenze prime, il che corrisponde alla presenza di un trend
          quadratico nelle variabili (espresse in livelli). Questi due casi sono
          specificati rispettivamente con le opzioni <literal>--crt</literal> e
          <literal>--ct</literal> del comando <xref linkend="cmd-vecm"/>.
	</para>
      </listitem>
    </orderedlist>

  </sect1>

</chapter>

<!-- Keep this comment at the end of the file
Local variables:
sgml-default-dtd-file:"../manual.ced"
mode: xml
sgml-parent-document:("../manual.xml" "book" "chapter")
End:
-->









