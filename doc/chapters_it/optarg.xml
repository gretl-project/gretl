<?xml version="1.0" encoding="iso-8859-1"?>

  <chapter id="optarg"><title>Opzioni, argomenti e percorsi</title>
  

  <sect1 id="optarg1"><title>gretl</title>

    <para>
      <command>gretl</command> (in MS Windows,
      <command>gretlw32.exe</command>)<footnote><para>In Linux viene installato
      uno script <quote>wrapper</quote> chiamato <filename>gretl</filename>.
      Questo script controlla se sia impostata la variabile di ambiente
      <literal>DISPLAY</literal> e in caso positivo lancia la versione grafica
      del programma, altrimenti lancia la versione a riga di comando,
      <filename>gretlcli</filename>.</para></footnote>.
    </para>

    <para>
      &mdash; Apre il programma e aspetta l'input dall'utente.
    </para>

    <para>
      <command>gretl</command> <replaceable>filedati</replaceable>
    </para>

    <para>
      &mdash; Avvia il programma caricando nello spazio di lavoro il file di
      dati indicato. Il file può essere in uno dei vari formati supportati
      (si veda la &GUG;): il programma cercherà di riconoscere il formato del
      file e di trattarlo in modo appropriato. Si veda anche la <xref
      linkend="path-search"/> per quanto riguarda la ricerca dei percorsi.
    </para>

    <para>
      <command>gretl --help</command> (o <command>gretl -h</command>)
    </para>

    <para>
      &mdash; Mostra un breve riepilogo delle modalità di uso ed esce.
    </para>

    <para>
      <command>gretl --version</command> (o <command>gretl
	-v</command>)
    </para>

    <para>
      &mdash; Mostra le informazioni sulla versione del programma ed esce.
    </para>

    <para>
      <command>gretl --english</command> (o <command>gretl
	-e</command>)
    </para>

    <para>
      &mdash; Forza l'utilizzo della versione inglese invece di quella italiana.
    </para>

    <para>
      <command>gretl --run</command>
      <replaceable>file</replaceable> (o <command>gretl
	-r</command> <replaceable>file</replaceable>)
    </para>

    <para>
      &mdash; Avvia il programma e apre una finestra che mostra il file di
	comandi indicato, pronto per essere eseguito. Si veda la <xref
	  linkend="path-search"/> per quanto riguarda la ricerca dei
	percorsi.
    </para>

    <para>
      <command>gretl --db</command>
      <replaceable>database</replaceable> (o <command>gretl
	-d</command> <replaceable>database</replaceable>)
    </para>

    <para>
      &mdash; Avvia il programma e apre una finestra che mostra il database
      indicato. Se i file del database (il file <filename>.bin</filename> e il
      relativo file <filename>.idx</filename>, si veda la &GUG;) non sono nella
      directory predefinita dei database, occorre specificare il percorso
      completo dei file.
    </para>

    <para>
      <command>gretl --dump</command> (o <command>gretl -c</command>)
    </para>

    <para>
      &mdash; Scrive le informazioni di configurazione del programma in un file
      di testo (il nome del file è mostrato nello standard output). Può essere
      utile nella risoluzione di problemi di funzionamento.
    </para>

    <para>
      <command>gretlw32 --debug</command> (o <command>gretlw32 -g</command>)
    </para>

    <para>
      &mdash; (solo MS Windows) Apre una finestra di terminale per mostrare
      eventuali messaggi inviati allo <quote>standard output</quote> o allo
      <quote>standard error</quote>. Questi messaggi, che di solito non sono
      visibili in Windows, possono essere utili per risolvere problemi di
      funzionamento.
    </para>

    <para>
      Varie funzionalità di <application>gretl</application> sono configurabili
      nel menù <guimenuitem>File, Preferenze</guimenuitem>
      menu.
    </para>  

    <itemizedlist>
	<listitem><para>La directory base per i file condivisi di
	    <application>gretl</application>.</para></listitem> 
	<listitem><para>La directory base dell'utente per i file relativi a
	    <application>gretl</application>.</para></listitem> 
	<listitem><para>Il comando per eseguire
	    <application>gnuplot</application>.
	  </para></listitem> 
	<listitem><para>Il comando per eseguire GNU R.</para></listitem>
	<listitem><para>Il comando per visualizzare i file DVI di
	    &tex;.</para></listitem> 
	<listitem><para>La directory in cui iniziare a cercare i database in
	    formato interno di
	    <application>gretl</application>.</para></listitem>
	<listitem><para>La directory in cui iniziare a cercare i database in
	    formato RATS 4.</para></listitem> 
	<listitem><para>Il nome host del server dei database di
	    <application>gretl</application> a cui
	    connettersi.</para></listitem> 
	<listitem><para>L'indirizzo IP e il numero di porta del proxy HTTP da
	    usare per contattare il server dei database, se necessario (se si è
	    dietro a un firewall).</para></listitem> 
	<listitem><para>La calcolatrice e l'editor di testi da eseguire dalla
	    barra dei comandi.
          </para></listitem> 
	<listitem><para>Il tipo di carattere a spazio fisso da usare nelle
	    finestre dei risultati di
	    <application>gretl</application>.</para></listitem> 
	<listitem><para>Il tipo di carattere da usare nei menù e negli altri
	    messaggi (nota: questo elemento non è presente se
	    <application>gretl</application> è compilato per il desktop
	    <application>gnome</application>, visto che la scelta dei caratteri
	    è gestita in modo centralizzato da
	    <application>gnome</application>).
	  </para></listitem>
    </itemizedlist>

    <para>
      Ci sono anche alcune caselle di testo: selezionando la casella
      <quote>esperto</quote>, vengono disabilitati alcuni messaggi di
      avvertimento che altrimenti verrebbero mostrati; selezionando la casella
      <quote>Avvisa in caso di aggiornamenti di gretl</quote>,
      <application>gretl</application> cercherà di connettersi al server dei
      database appena avviato;  de-selezionando <quote>Mostra la barra degli
      strumenti di gretl</quote>, la barra degli strumenti sarà disabilitata.
      Se l'impostazione locale del proprio sistema non usa il punto
      (<quote><literal>.</literal></quote>) come separatore delle cifre
      decimali, de-selezionando <quote>Usa le impostazioni locali
      per il punto decimale</quote>, <application>gretl</application> userà
      comunque il carattere punto.
    </para>

    <para>
      Nella sezione <quote>Percorso per Apri/Salva</quote> è possibile impostare
      il percorso usato in modo predefinito da <application>gretl</application>
      quando si chiede di aprire o salvare un file: o la directory
      <application>gretl</application> dell'utente o la directory di lavoro
      attuale.
    </para>
    
    <para>
      Nella sezione <quote>Generali</quote> è possibile scegliere l'algoritmo
      usato da <application>gretl</application> per calcolare le stime dei
      minimi quadrati. La scelta predefinita è la decomposizione di Cholesky,
      che è veloce, relativamente economica in termini di requisiti di memoria,
      e accurata per la maggior parte delle applicazioni. L'alternativa
      è la decomposizione QR, che è computazionalmente più impegnativa e
      richiede una maggiore quantità di memoria, ma è più accurata.
      È poco probabile dover avere bisogno dell'accuratezza ulteriore fornita
      dalla decomposizione QR, a meno che non si abbia a che fare con dati
      molto mal-condizionati e si sia interessati a valori dei coefficienti
      e degli errori standard precisi per più di 7 cifre decimali<footnote>
      <para>L'uso della decomposizione QR è attivabile anche impostando la
      variabile di ambiente <varname>GRETL_USE_QR</varname> a un qualsiasi
      valore non nullo.</para>
	</footnote>.
    </para>

    <para>
      Le impostazioni scelte in questo modo sono salvate in modo diverso
      a seconda del sistema. In MS Windows sono scritte nel registro di Windows.
      Nel desktop di <application>gnome</application> sono scritte nel file
      <filename>.gnome/gretl</filename> nella home directory dell'utente.
      Altrimenti vengono scritte in un file chiamato
      <filename>.gretlrc</filename> nella home directory dell'utente.
    </para>

  </sect1>

  <sect1 id="optarg2"><title>gretlcli</title>

    <para><command>gretlcli</command></para>

    <para>&mdash; Apre il programma e attende l'input dall'utente.
    </para>

    <para><command>gretlcli</command>
      <replaceable>filedati</replaceable></para>  

    <para>
      &mdash; Avvia il programma caricando nello spazio di lavoro il file di
	dati indicato. Il file può essere nel formato interno di
	<application>gretl</application>, in formato CSV, o BOX1 (si veda la
	&GUG;). Il programma cercherà di riconoscere il formato del file e di
	trattarlo in modo appropriato. Si veda anche la
      <xref linkend="path-search"/> per quanto riguarda la ricerca dei percorsi.
    </para>
    
    <para><command>gretlcli --help</command> (o <command>gretlcli
	-h</command>)</para>

    <para>
      &mdash; Mostra un breve riepilogo delle modalità di uso ed esce.
    </para>

    <para><command>gretlcli --version</command> (o <command>gretlcli
	-v</command>)</para>

    <para>
      &mdash; Mostra informazioni sulla versione del programma ed esce.
    </para>

    <para><command>gretlcli --english</command> (o <command>gretlcli
	-e</command>)</para>

    <para>
      &mdash; Forza l'utilizzo della versione inglese invece di quella italiana.
    </para>

    <para>
      <command>gretlcli --run</command>
      <replaceable>file</replaceable> (o <command>gretlcli
	-r</command> <replaceable>file</replaceable>)</para>

    <para>&mdash; Esegue i comandi contenuti nel <replaceable>file</replaceable>
      e aspetta ulteriore input dalla riga di comando.  Si veda la <xref
      linkend="path-search"/> per quanto riguarda la ricerca dei
      percorsi.</para>

    <para><command>gretlcli --batch</command>
      <replaceable>file</replaceable> (o <command>gretlcli
	-b</command> <replaceable>file</replaceable>)</para>

    <para>&mdash; Esegue i comandi contenuti nel <replaceable>file</replaceable>
      ed esce; tipicamente questa opzione viene usata redirigendo i risultati del
      comando a un file.  Si veda la <xref
      linkend="path-search"/> per quanto riguarda la ricerca dei
      percorsi.</para>

    <para>Se si usano le opzioni <command>--run</command> e
      <command>--batch</command>, il file richiamato deve indicare un
      file di dati da aprire. Ciò può essere fatto usando il comando
      <command>open</command> nel file.
    </para>

  </sect1>

  <sect1 id="path-search"><title>Ricerca dei percorsi</title>

    <para>Quando si indica il nome di un file di dati o di comandi a
      <application>gretl</application> o <application>gretlcli</application>
      sulla riga di comando, il file viene cercato nel modo seguente:</para>

    <orderedlist>
	<listitem><para><quote>Esattamente come viene specificato</quote>.
	    Ossia, nella directory di lavoro attuale, o, se viene indicato un
	    percorso completo, nella posizione indicata.</para>
	</listitem>
	<listitem><para>Nella directory gretl dell'utente (si veda la <xref
	      linkend="tab-path"/> per i valori predefiniti).</para>
	</listitem>
	<listitem><para>In qualsiasi directory immediatamente contenuta nella
            directory gretl dell'utente.</para>
        </listitem>
        <listitem><para>Nel caso di un file di dati, la ricerca continua nella
            directory principale dei dati di <application>gretl</application>.
            Nel caso di un file di comandi, la ricerca continua nella directory
            principale dei file di comandi. Si veda la <xref
              linkend="tab-path"/> per i valori predefiniti.
            <filename>PREFISSO</filename> indica la directory base scelta al
            momento dell'installazione di
            <application>gretl</application>.</para>
	</listitem>
	<listitem><para>Nel caso dei file di dati, la ricerca continua in tutte
	    le directory immediatamente contenute nella directory principale dei
            dati.</para>
	</listitem>
    </orderedlist>

    <table id="tab-path" frame="none">
      <title>Impostazioni predefinite dei percorsi</title>
      <tgroup cols="3">
	<thead>
	  <row>
	    <entry>&nbsp;</entry>
	    <entry>Linux</entry>
	    <entry>MS Windows</entry>
	  </row>
	</thead>
	<tbody>
	  <row>
	    <entry>Directory gretl dell'utente</entry>
	    <entry><filename>$HOME/gretl</filename></entry>
	    <entry><filename>PREFISSO\gretl\user</filename></entry>
	  </row>
	  <row>
	    <entry>Directory di sistema dei dati</entry>
	    <entry><filename>PREFISSO/share/gretl/data</filename></entry>
	    <entry><filename>PREFISSO\gretl\data</filename></entry>
	  </row>
	  <row>
	    <entry>Directory di sistema dei file di comandi</entry>
	    <entry><filename>PREFISSO/share/gretl/scripts</filename></entry>
	    <entry><filename>PREFISSO\gretl\scripts</filename></entry>
	  </row>
	</tbody>
      </tgroup>
    </table>

    <para>Non è quindi necessario specificare il percorso completo per un file
      di dati o di comandi, a meno che non si voglia fare a meno del
      meccanismo di ricerca automatico (ciò è valido anche per
      <application>gretlcli</application> quando si indica un nome di file
      come argomento per il comando <command>open</command> o il comando
      <command>run</command>)</para>

    <para>Quando un file di comandi contiene l'istruzione di aprire un file di dati,
      l'ordine di ricerca per il file è quello descritto sopra, con l'eccezione
      che il file viene cercato anche nella directory che contiene lo script,
      immediatamente dopo aver tentato di cercare il file di dati 
      <quote>esattamente come viene specificato</quote>.
    </para>

      <sect2 id="MS-behave"><title>MS Windows</title>

	<para>
	  In MS Windows le informazioni di configurazione per
	  <application>gretl</application> e <application>gretlcli</application>
	  sono scritte nel registro di Windows. Quando
	  <application>gretl</application> è installato per la prima volta,
	  viene creata un'appropriata serie di voci di registro; successivamente
	  le impostazioni possono essere modificate attraverso il menù
	  <guimenuitem>File, Preferenze</guimenuitem> di
	  <application>gretl</application>. Se occorre eseguire delle modifiche
	  manuali dei valori, le voci di registro possono essere trovate (usando
	  il programma standard di Windows
	  <application>regedit.exe</application>) sotto
	  <literal>Software\gretl</literal> in
	  <literal>HKEY_LOCAL_MACHINE</literal> (la directory principale di
	  <application>gretl</application> e i percorsi per i vari programmi
          ausiliari) e in <literal>HKEY_CURRENT_USER</literal> (tutte le altre
          variabili configurabili).
	</para>

    </sect2>
  </sect1>

  </chapter>


<!-- Keep this comment at the end of the file
Local variables:
sgml-default-dtd-file:"../manual.ced"
mode: xml
sgml-parent-document:("../manual_it.xml" "book" "chapter")
End:
-->

