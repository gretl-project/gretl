/* 
   Temporary header to share a definition between geneval.c
   and usermat.c: with USE_CIDX set to non-zero we assume
   that user-specified indices for complex matrices are
   given in 16-byte mode.
*/

#define USE_CIDX 1
