
Command   Option            Effect                      Short form

arma:     
         --x-12-arima       use X-12-ARIMA                 -x              
         --verbose          print iteration details        -v

coint2:
         --verbose          print details of VARs          -o

eqnprint, 
tabprint:
         --complete         complete TeX document          -o
         -f                 specify filename               -f

fcasterr: 
         --plot             display graph                  -o

gnuplot:  
         --with-lines       plot with lines                -o
         --with impulses    plot with impulses             -m
         --suppress-fitted  do not show fitted line        -s
         --dummy            factor separation              -z

import:
         --box1             import BOX1 data               -o

leverage:
         --save             save values to dataset         -o

lmtest:
         --logs             non-linearity (logs)           -l
         --squares          non-linearity (squares)        -s
         --white            heteroskedasticity             -w
         --autocorr         autocorrelation                -o

meantest:
         --unequal-vars     assume unequal variances       -o

ols:
         --robust           robust standard errors         -r
         --vcv              print covariance matrix        -o
         --quiet            don't print results            -q

outfile: 
         --write            open file for writing          -w
         --append           append to existing file        -a
         --close            close file                     -c

panel:
         --time-series      stacked time series            -s
         --cross-section    stacked cross-sections         -c

pca:      
         --save             save useful eigenvectors       -o
         --save-all         save all eigenvectors          -a

pergm: 
         --bartlett         use Bartlett lag window        -o

plot:
         --one-scale        don't scale variables          -o

print:
         --byobs            variables in columns           -o
         --ten              use 10 significant figures     -t

smpl: 
         --dummy            restrict using dummy var       -o
	 --no-missing       only complete observations     -m
         --restrict         use boolean criterion          -r

spearman:
         --verbose          print original, ranked data    -o

square:
         --cross            generate cross-products        -o

store:
         --csv              comma-separated values         -c
         --traditional      traditional ESL format         -t
         --gnu-octave       GNU Octave format              -m
         --gnu-R            GNU R format                   -r
         --gzipped          gzip compression               -z

var:
         --quiet            don't print results            -q

model commands 
in general:
         --vcv              print covariance matrix        -o
