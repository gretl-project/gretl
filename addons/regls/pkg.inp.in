set echo off
set messages off

string topsrc = "@top_srcdir@"
string xsd = topsrc ~ "/share/functions/gretlfunc.xsd"
string pkgdir = topsrc ~ "/addons/regls"

include @pkgdir/regls.inp
makepkg regls.gfn --index --translations --quiet --schema="@xsd"
