set verbose off
include dbnomics.gfn

prov = "AMECO"
bundle b = dbnomics_tree_for_provider(prov, 1)
print b
bundle c = dbnomics_tree_get_child(b, 1)
print c
if c.type == "cat_list"
   bundle d = dbnomics_tree_get_child(c, 1)
   print d
endif
if d.type == "cat_list" || d.type == "db_list"
   bundle e = dbnomics_tree_get_child(d, 1)
   print e
endif
